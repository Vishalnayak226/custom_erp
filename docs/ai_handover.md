# AI Handover Note: Developer Guide

This document provides system setup maps, port directories, command recipes, and database configurations for the next AI agent or developer taking over code construction.

---

## 1. System Environment & Port Bindings

*   **Go Runtime**: Portable Go 1.22.5 is extracted under `$env:USERPROFILE\go-portable\go`.
*   **PostgreSQL Server**: Portable PostgreSQL 16.3 is extracted under `$env:USERPROFILE\pg-portable\pgsql`.
*   **PostgreSQL Port**: Runs on port **`5435`** (using trust authentication).
*   **PostgreSQL Cluster Dir**: Data directory is located at `$env:USERPROFILE\pg-data`.
*   **Database User & DB**: User is `postgres` (no password). Database name is `custom_erp`.
*   **Web Application Port**: Serves static pages and HTTP API router on port **`8080`** (`http://localhost:8080`).
*   **Login required**: as of 2026-07-12 the app requires signing in — see §6/§7. Dev credentials are in `DEV_CREDENTIALS.local.txt` at the project root (gitignored, not in this doc).
*   **Easiest way to start/stop everything**: `.\manage.ps1` (interactive menu) or `.\manage.ps1 start` / `stop` / `restart` / `status` / `logs` — see §3.C note on a known hang with this in sandboxed/background shell contexts.

---

## 2. Core Repository Map

*   [go.mod](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/go.mod): Go module configuration (`module custom_erp`).
*   [cmd/server/main.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/cmd/server/main.go): Real entrypoint - a thin launcher that calls `internal/server.Run()`. Build with `go build -o erp-server.exe ./cmd/server` (not a bare `go build -o erp-server.exe` - the module root is no longer package `main` since the Stage 19 folder restructuring, 2026-07-19).
*   [internal/server/](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/internal/server/): Web server API router, Dynamic Tenant resolution, and Panic Recovery Middleware - was a single root-level `main.go` before 2026-07-19, split by domain into `routes.go` (route table + `Run()`), `middleware.go`, and `handlers_*.go` files (see `README.md`'s Project Structure for the full file list). Same package throughout (`package server`), so this was a pure reorganization - no behavior change.
*   [db/db.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/db/db.go): Connection pool initialization and dynamic tenant schema-switching handler.
*   [db/migration.sql](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/db/migration.sql): Database tables, Chart of Accounts, user mappings, and default configurations.
*   [engines/numbering.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/numbering.go): Sequence numbering generator.
*   [engines/labels.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/labels.go): Translation CRUD mappings API.
*   [engines/logs.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/logs.go): Audit trail logger and panic recovery recorder.
*   [engines/inventory.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/inventory.go): Barcoded stock counts, availability read models, and temporary reservations.
*   [engines/finance.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/finance.go): Balanced double-entry GL journal ledger postings.
*   [engines/sourcing.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/sourcing.go): Rule-driven order routing, mapping, and webhook idempotency checks.
*   [engines/fulfillment.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/fulfillment.go): Store picking tasks, re-routing on task reject, and Return Anywhere.
*   [engines/marketplace.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/marketplace.go): Marketplace settlements reconciliation and logistics bookings dispatch tracker.
*   [engines/optimization.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/optimization.go): Demand forecasting, replenishment suggestions, and task SLA checks. Has a known bug — see §6.
*   [engines/saas.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/saas.go): Tenant provisioning (schema cloning) and per-tenant feature flags.
*   [engines/outbox.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/outbox.go): Real-time event-driven outbox delta sync poller, plus integration log query/retry.
*   [engines/mfa.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/mfa.go): TOTP MFA (RFC 6238, stdlib-only) — enrollment, activation, verification; required for HR/Admin roles.
*   [engines/approval.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/approval.go): Maker-checker approval/workflow engine — amount-slab+role+location routing, submit/decide, row-locked against double-decision races.
*   [engines/reports.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/reports.go): Current Stock, Sales Register, Vendor Ledger, and Payables Ageing reports.
*   [engines/rfq.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/rfq.go): Vendor RFQ quote comparison and winning-quote selection (transactional select/reject/close).
*   [engines/gst.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/gst.go): GST calculation (CGST/SGST intra-state split, IGST inter-state) — calc-only, not full e-invoicing.
*   [engines/stickers.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/stickers.go): Sticker/barcode label printing and print history — text labels, not scannable symbology.
*   [engines/hr.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/hr.go): Employee access-link sync and payroll export.
*   [engines/assets.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/assets.go): Fixed Asset lifecycle — capitalize, straight-line depreciation, transfer, dispose, asset register.
*   [engines/expense.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/expense.go): Expense claim controls, verification, and payment (posts GL via the approval engine).
*   [engines/loyalty.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/loyalty.go): CRM/Loyalty point ledger — earn/redeem/balance, append-only (scoped MVP, no campaigns/segmentation).
*   [engines/manufacturing.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/manufacturing.go): Single-level BOM + linear Production Order (material issue, FG receipt) — scoped MVP, no routing/MRP/QC.
*   [engines/engines_test.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/engines/engines_test.go): Unit-style tests calling engine functions directly (not through HTTP) - despite the name, these don't exercise `internal/server`'s handlers/middleware at all.
*   [internal/server/server_test.go](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/internal/server/server_test.go): The project's actual HTTP-level integration test (added 2026-07-12, renamed from `main_test.go` in the 2026-07-19 restructuring) - drives real handlers via `httptest`, no real socket needed. Lives alongside the handlers in `internal/server` (package `server`) because only that package can call its own unexported handlers.
*   [index.html](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/public/index.html): ERP UI layout.
*   [app.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/ERP/public/app.js): Application UI routing and translation state engine.

---

## 3. Development Command Recipes

All commands should be executed from the repository root directory `c:\Users\ABCD\Documents\Antigravity Projects\ERP`:

### A. Run Unit Tests
To run unit tests verifying numbering and translation engines:
```powershell
& "$env:USERPROFILE\go-portable\go\bin\go.exe" test ./...
```

### B. Compile Backend Binaries
For local dev/testing (keeps debug symbols - easier to troubleshoot a panic's stack trace):
```powershell
& "$env:USERPROFILE\go-portable\go\bin\go.exe" build -o erp-server.exe ./cmd/server
```
For a release build (strips symbols/debug info, ~30% smaller): `.\manage.ps1 release`, or directly:
```powershell
& "$env:USERPROFILE\go-portable\go\bin\go.exe" build -ldflags="-s -w" -o erp-server.exe ./cmd/server
```
(Since the 2026-07-19 folder restructuring, `./cmd/server` is required - the repo root itself is no longer package `main`, `internal/server` is.)
Windows locks a running `.exe`, so `manage.ps1 release` stops the server first if it's up (and does not restart it - run `start` yourself when ready).

### C. Start PostgreSQL (Foreground Backgrounded)
To start the database server. Note that starting `postgres.exe` directly as a long-running foreground task is required in sandboxed shells (as `pg_ctl start` spawned detached services are subject to process group termination upon session end):
```powershell
& "$env:USERPROFILE\pg-portable\pgsql\bin\postgres.exe" -D "$env:USERPROFILE\pg-data" -p 5435
```
**Known issue (found 2026-07-12):** `manage.ps1`'s `pg_ctl start ... -w` can hang indefinitely when that script itself is run as a backgrounded/sandboxed task (the underlying Postgres instance actually starts fine — confirmed via `pg_isready`/`psql` — it's specifically `pg_ctl -w`'s own readiness wait that doesn't return in that context). If `manage.ps1 start` appears stuck, check `pg_isready -p 5435` directly; if that reports ready, the DB is fine and you can proceed (e.g. start `erp-server.exe` directly) without waiting on the stuck script.

### D. Start Go ERP Web Server
To launch the compiled server binary:
```powershell
& "c:\Users\ABCD\Documents\Antigravity Projects\ERP\erp-server.exe"
```

---

## 4. Multi-Tenant Development Standards (Next Phases)
When building subsequent modules (e.g. Stage 3: Master Packages, Stage 5: GRN & Inventory):
1.  **Always Resolve Tenant**: Inspect `Resolved-Tenant-ID` header inside Go API handlers.
2.  **Scope Queries using Local Search Path**: In your database transaction, always run `db.SetSearchPath(tx, schema)` before issuing any queries. Never write hardcoded table queries without schema parameters unless scoped.
3.  **Audit Logs**: Log all document transitions, submissions, and approvals using backend triggers or explicit audit statements.
4.  **Sequence Code Generation**: Always request document numbering codes via `engines.GenerateSequence()` or construct variant concatenations using `engines.GenerateVariantCode()`.

---

## 5. Omnichannel Scale Architecture (Event-Driven & Outbox Patterns)

To support scaling from 1 store to 2,000 stores without transaction database lockouts:
1.  **Outbox Pattern for External Sync**: User-facing transactions must NEVER make synchronous HTTP calls to external channels (Shopify, payment gateways, GST APIs).
    *   Insert the record into the primary table and an event log into the `integration_event_outbox` table **within the same SQL transaction**.
    *   The background worker polls the outbox and publishes the event to the external integration gateway asynchronously.
2.  **Inventory Availability Calculations**: Do not query the raw transaction ledger directly for real-time channels. Expose calculated Available-to-Sell (ATS) stock using the read model:
    `Available to Sell = Available - Reserved - Safety Stock - Channel Holds`
3.  **Strict Idempotency keys**: Validate signature, timestamp, and unique UUID identifiers on all webhook payloads to prevent duplicate order or invoice creations.

---

## 6. Version Control & Handover Status

*   **Remote GitHub URL**: `https://github.com/Vishalnayak226/custom_erp.git` (Branch: `main`)
*   **Build Handover Milestone**:
    - **Phases 1-7** (Core Foundation, Single Vertical Pilot, Omnichannel Sync, Store Fulfillment, Concurrency Scale Testing, Marketplace Expansion, and Advanced Optimization Engines) are built and pushed to GitHub. Phase 7 has a known functional bug — see below.
    - **Real login flow (2026-07-12)**: closed `hardening_roadmap.md` Phase 1.1 — the app now requires signing in. Added a login screen, reset all 4 seed users to unique credentials (`DEV_CREDENTIALS.local.txt`, gitignored — dev-only, rotate before real use), and `apiMiddleware` now rejects any request without a valid token except `/api/v1/login` itself. Token expiry (Phase 1.4) is also closed — `engines/auth.go`'s `tokenTTL()` embeds an `exp` claim (default 24h, `JWT_EXPIRY_HOURS` overridable) and `ParseToken` validates it.
    - **SaaS Provisioning & Feature Flags**: Built schemas cloning structures, seeding default metadata, and setting flag boundaries per tenant. Provisioning now generates a unique, freshly-random bcrypt-hashed admin credential per tenant (`generateRandomPassword()` in `engines/saas.go`) rather than cloning a shared placeholder hash. Feature flags are still set-only — `IsFeatureEnabled` isn't checked by any route handler yet, so flags don't gate anything in practice (tracked in `docs/pdf_blueprint_gap_analysis.md` Phase A).
    - **Error Logs Hub & Outbox Retries**: Built API endpoints for log querying and failed integration event resets. The frontend Log Hub view does not yet call these endpoints (no integration-payload list or retry button in the UI). **Webhook signature verification was not actually implemented** — no signature check exists on the Shopify order webhook or any other callback handler.
    - **Premium UI Overhaul**: Created a high-fidelity Stripe/Linear-style dashboard with KPI tickers, soft glowing focus borders, and centered glassmorphism dialog modals. The custom dialog migration covers `alert`/`confirm`; several `prompt()` calls (DocType Builder, Prefix Config edit) are still raw browser prompts.
    - **Stage 13 (2026-07-12 through 2026-07-17): closed the business-user-facing gap identified in `pdf_blueprint_gap_analysis.md`.** Built one item at a time (each with its own commit, live-verified via a throwaway server instance before committing — see `docs/erp-stress-testing-notes.md`-style methodology in memory): security headers + feature gating (13.1-13.3), POS/Finance/Fulfillment/Marketplace screens for existing backend (13.4-13.7 — also fixed two location-filter bugs where `location` vs `location_code` field-name mismatches hid records from non-admins), TOTP MFA for HR/Admin roles (13.9-ish — `engines/mfa.go`), the maker-checker approval/workflow engine (13.8, reused by ExpenseClaim later — `engines/approval.go`, later hardened against a TOCTOU double-decision race by a concurrent session, commit `bffea51`), Vendor/Customer masters + Item GST fields (13.9-13.10), report catalog (13.11 — `engines/reports.go`), RFQ/vendor quote comparison (13.12 — `engines/rfq.go`), per-API-type rate limiting keyed `ip:category` instead of just `ip` (13.14 — fixes cross-endpoint 429 interference), sticker/barcode label printing (13.15 — `engines/stickers.go`, text labels not scannable symbology). Then, driven by a newer blueprint PDF (`Inhouse_ERP_Module_Blueprint_CRM_HR_Expense_Assets_Manufacturing_RateLimit_Barcode.pdf`): HR Foundation (13.13a — `engines/hr.go`), Fixed Asset Management (13.13b — `engines/assets.go`), Expense Management (13.13c — `engines/expense.go`, reuses the approval engine), CRM/Loyalty scoped MVP (13.13d — `engines/loyalty.go`, point ledger only, no campaigns), Manufacturing scoped MVP (13.13e — `engines/manufacturing.go`, single-level BOM, no routing/MRP/QC). Full scope decisions and live-verification writeups for every item are in `docs/micro_checklist.md`, which is the current source of truth for what's built — this section is a summary, not the detail.
    - **Stage 14-17 (2026-07-18 through 2026-07-19).** Stage 14: control plane/deployment pipeline (`promote.ps1`/`manage.ps1 -Env`/`environments.json`), module governance, patch automation, extension isolation, security hardening (account lockout, Shopify webhook HMAC verification), Go 1.22.5→1.22.12. Containerization was built on explicit request then **reverted on explicit request 2026-07-19** — standing policy is no Docker dependency in this project; `promote.ps1`/`manage.ps1 -Env` is the one real deployment path, not Docker. Stage 15: PIM Foundation MVP + V2 (`engines/pim.go`/`pim_media.go`/`pim_publish.go` — Family/Attribute framework, approval-gated ProductContent, completeness engine, this codebase's first file-upload infra, channel-publish queue with a stub connector, CSV import/export preview). Stage 16: real Shopify/BigCommerce/Magento connectors (`engines/connector*.go`, code-complete, live-store verification still deferred pending real dev-store credentials), PIM dashboard/bulk-edit/reports/field-permissions (`engines/pim_reports.go`/`pim_bulk.go`/`field_permissions.go`), plus Stage 9 integrations (`engines/clevertap.go`/`pinelabs.go`/`unicommerce.go`) and Log Hub frontend wiring. Stage 17: soft-delete (17.1, additive `deleted_at` tombstone), CSV formula-injection sanitization (17.2), backup/restore baseline (17.3 — `manage.ps1 backup`/`restore -Env`, see `docs/backup_restore.md`), accounting-period control (17.4 — `engines/accounting_periods.go`), GST posting enforcement at PO creation and checkout (17.5 — `engines/gst.go`), transfer-order dispatch/receive (17.6 — `engines/transfer_orders.go`, new `in_transit` column), purchase requisition workflow (17.7 — `engines/procurement.go`), vendor invoice 3-way match + payment (17.8 — `engines/vendor_invoice.go`), and Location/LegalEntity/Department/CostCenter masters (17.9 — `engines/location_masters.go`, 103 legacy codes seeded). Full detail and a chronological summary: `docs/project_ledger.md` §13.
    - **Stage 17.10-17.11 (2026-07-19, uncommitted at time of writing — see below).** Both items were blocked on real-world input (operational contacts; non-production platform credentials), not implementation — everything buildable ahead of that input was built and live-verified this session. **17.10**: `docs/operations/incident_runbook.md` (P0-P3, rollback, log locations, escalation-contacts table left as an explicit `[NOT YET FILLED IN]` placeholder); `engines/alerting.go` (`SendOpsAlert` → Slack/Teams-compatible webhook via `OPS_ALERT_WEBHOOK_URL`, safe no-op when unset, never sends a full stack trace externally; `StartAlertMonitor` for sustained error rate); panic recovery (`engines/logs.go`) and failed backups (`manage.ps1`'s new `Send-OpsAlert`) both wired in; live-verified against a local mock webhook receiver (no real Slack/Teams destination existed to point at — only the URL differs for a real one). **17.11**: `scripts/verify_connector_live.ps1` + `docs/operations/connector_live_verification.md` — drives a real Shopify/BigCommerce/Magento publish once given real credentials (`CONNECTOR_CREDENTIALS.local.json`, gitignored) and a pre-approved disposable Item; not run end-to-end since no real store credentials exist in this environment. Full detail: `docs/project_ledger.md` §14, `docs/micro_checklist.md` Stage 17 (17.10/17.11 entries spell out exactly what's built vs. what the user still needs to supply).
    - **Stage 18 (2026-07-19, built + live-verified).** Per explicit user request: audit and add dropdown/autosuggest UX to free-text master-reference fields. Audit found the two originally-named examples (Chart of Accounts, GRN) don't exist as buildable screens; scoped instead to real free-text Vendor/Location/Customer/Item/Employee fields found across PurchaseOrder/POS/RFQ/HR/Assets/Expenses/Manufacturing. Stayed UI-only by design (no `fieldtype` schema changes, no new server-side validation — mirrors Stage 17.9's own decision on the same question). Built one reusable `attachTypeahead()` component (`public/app.js`, backed by the existing `GET /api/v1/doc/{doctype}?q=...` search) and wired it into 12 fields across 7 screens, plus fixed Marketplace's hardcoded channel dropdown to also pull real `Channel` records. **Two real bugs found and fixed via live Playwright verification against the actual dev server/DB**: (1) a logic bug in the new component itself where `openMenu()` unintentionally wiped its own results before rendering them (fixed); (2) a **pre-existing Stage 17.9 bug** — `Location`/`LegalEntity`/`Department`/`CostCenter` were gated to a `'core'` module that was never registered in `public.modules` or granted to any tenant, so those four doctypes have been returning `403` on every read/write through the generic doc endpoint, for every role, on every tenant, since Stage 17.9 shipped; fixed via new `db/migrations_stage18_core_module_fix.sql`, applied live to `custom_erp`. **Found but deliberately not fixed**: `Store Manager`/`Cashier` (the roles that actually use PO/POS screens) have no read access to `Vendor`/`Item`/`Customer` in `role_permissions`, so the new pickers are code-correct but currently non-functional for those roles — flagged `[needs product decision]` in `docs/micro_checklist.md` Stage 18 rather than silently expanding RBAC. Full detail: `docs/project_ledger.md` §16.
    - **Stage 19 (2026-07-19, completed).** Documentation suite + folder restructuring, per explicit user request. `docs/ERP_BLUEPRINT.md` (full project snapshot for external/AI review). Go folder restructuring: `main.go` moved from repo root into `internal/server/` (`package server`, split into 8 domain files), new thin `cmd/server/main.go` entrypoint — the user explicitly chose this full restructuring over a lighter-touch option when asked about the regression risk. Every build command updated (`manage.ps1`, `promote.ps1`) and re-verified (`go build`/`vet`/`test ./... -p 1` clean, live smoke-tested on a throwaway port). `docs/` reorganized into `architecture/`/`specs/`/`operations/`/`requirements/`/`guides/` subfolders (the "big 4" — this file, `micro_checklist.md`, `project_ledger.md`, `ERP_BLUEPRINT.md` — stay at `docs/` root); nothing deleted, every cross-reference fixed. New `docs/requirements/BRD.md`/`PRD.md` and `docs/guides/USER_GUIDE.md`/`ADMIN_GUIDE.md`, grounded in three external planning references the user pointed to mid-session. Full detail: `docs/project_ledger.md` §15, `docs/micro_checklist.md` Stage 19.
    - **Stage 20 (2026-07-19, planning only — no build work yet).** User supplied an external `ERP_Full_Fledged_Maturity_Master_Plan.pdf`, generated by the user from this project's own docs (not an independent audit). Reviewed in full and reconciled against live repo/git state: new `docs/specs/erp_maturity_master_plan.md` holds the phase framework, the reconciled P0/P1 gap table (one PDF claim was stale — it listed data-entry typeahead as "not started" when Stage 18 above had already closed it, just uncommitted at the time the PDF was generated), and an execution split into Track A (blocked on user-supplied input) vs. Track B (buildable now, no external input needed). Per explicit follow-up request, both tracks are now broken down into 40 individually-numbered, build-sized items (20.1-20.40) in `docs/micro_checklist.md` — Track A (20.1-20.5: escalation contacts, webhook URL, connector credentials, hosting decision, security-review engagement) and Track B grouped as POS Maturity (20.6-20.15), WMS Maturity (20.16-20.24), Finance Maturity (20.25-20.34), and Reports Engine (20.35-20.40), each item noting where it should reuse an existing engine/pattern rather than add something new (this repo's lightweight/future-proof/solid first principle, `CLAUDE.md`) and flagging `[needs product decision]`/`[needs user input]` where one blocks it. No item has been started yet — user said "just wanted the plan for now" before this breakdown, then asked for the breakdown itself without yet greenlighting any specific item to build.
    - **Stage 20 Track B.1 (2026-07-20, built + live-verified, run concurrently with the Stage 21 session below).** User asked to start building Stage 20's open items and picked POS Maturity (Track B.1, 20.6-20.15) as the first track. New `POSProfile` master (pure metadata registration) and `POSSession` doctype (Open/Closed cashier sessions, writable only through new dedicated `engines/pos_session.go` + `/api/v1/pos/session/{open,close,current}` endpoints, never the generic doc API); `handleCheckout` now requires an Open session for the acting cashier/location, and session close computes a counted-vs-expected cash variance server-side. `PostSalesFinanceBooking` now posts to a payment-mode-specific GL clearing account (1100 Cash / new 1101 Card / new 1102 UPI) instead of always 1100. A discount above a configurable percentage now routes through the *existing* `engines/approval.go` maker-checker engine before the sale completes (new shared `engines.FinalizePOSCheckout`, called both by the normal synchronous path and by `handleDecideApproval` on an Approved decision) — found and fixed a real latent bug while wiring this: `POSCart`'s `created_by` was hardcoded to `'system'` for every sale ever, which would have silently defeated the maker-checker same-user-can't-approve-their-own-submission check for this exact feature. 20.11 (POS-side return entry point), audited-and-confirmed-already-satisfied 20.12/20.15, and 20.14 (receipt printing, reusing Stage 13.15's print pattern) also closed this pass; 20.13 (offline queue) deliberately left open pending a product decision. New `db/migrations_stage20a_pos_maturity.sql`, applied live to `custom_erp`. Full detail: `docs/project_ledger.md` §18, `docs/micro_checklist.md` Stage 20 Track B.1.
    - **Stage 20 Track B.2 (2026-07-20, built + live-verified, immediately after Track B.1 in the same session).** WMS Maturity: `Bin` master (pure metadata) and a new `tenant_default.bin_stock` table (a bin+condition breakdown of the same on-hand `inventory_availability` already tracks, never a second source of truth) back putaway (`engines.PutawayToBin`), a computed bin-grouped/walk-route-sorted pick list for `FulfillmentTask`s, and Good/Damaged/QC-Hold/RTV condition transitions that keep `inventory_availability.available` in sync. `TransferOrder` gains an optional `Approved`→`Packed` box-mapping step before dispatch (additive - direct Approved→Dispatch still works). New `CycleCountLine` doctype reuses the Stage 3.3 `BulkImportCSV` engine verbatim for physical-count upload and the existing `engines/approval.go` maker-checker engine for variance approval, mirroring Track B.1's POS discount-gate pattern exactly. **A real, serious bug found and fixed during live verification**: the shared quantity-parsing helper didn't handle CSV-imported string values, silently reading every imported count as zero and posting a **-100** inventory adjustment where **-10** was correct - fixed and re-verified end-to-end (zero-variance, shortfall, and overage cases, each with an independent second approver) before this was considered done. New `db/migrations_stage20b_wms_maturity.sql`, applied live to `custom_erp`. Full detail: `docs/project_ledger.md` §20, `docs/micro_checklist.md` Stage 20 Track B.2.
    - **Nav redesign + renames (2026-07-20, built + live-verified, after Track B.2 in the same session).** User request, mid-session, screenshotting the live app: "DocType Builder" → "Database Schema Design" (user's own suggested name) and "Log Hub" → "Activity Log", applied everywhere user-facing and in current-state docs (historical build-record mentions deliberately untouched, matching this repo's own convention). Sidebar collapsed from ~27 flat top-level links into 12 module-grouped entries (POS, Finance, Fulfillment & Marketplace, Procurement, Inventory & WMS, HR & Assets, Admin & Settings as hover flyouts; Dashboard/Reports/Manufacturing/PIM standalone; Master Definition's existing dynamic dropdown unified into the same flyout mechanism), matching a reference screenshot the user provided. Every existing screen's id/click-handler/`renderView()` call was left untouched - only the surrounding markup and show/hide mechanism changed. **A real bug found and fixed during live Playwright verification**: the flyout's `max-height` was a flat viewport-height value that ignored the trigger's own vertical position, so the 25-item Master Definition flyout (anchored low in the list) ran off the bottom of the viewport - fixed to compute available space per-instance. Full detail: `docs/project_ledger.md` §21.
    - **Follow-up, then reverted (2026-07-20, same day, live user screenshots)**: tried a `.flyout-backdrop` dimming the content pane while a flyout was open (reusing `.modal-overlay`'s opacity-fade), to stop the flyout visually colliding with the screen underneath it. User feedback after seeing it live: the fade-in/out on every hover felt like an unwanted "sunrise/sunset" transition - reverted in full; the flyout's own `z-index:100` opaque card was already enough to sit on top, no backdrop needed. Database Schema Design's doctype list (`renderDocTypeBuilderView`) went through two iterations: first grouped under module headings, then - per the user's own live follow-up ("Only module will be there. I will hover and select") - converted to a real hover-flyout reusing the sidebar's own `setupModuleFlyouts()` (made idempotent via a `data-flyoutBound` guard so it can be safely re-run for this view's own dynamically-injected `.has-flyout` markup without double-binding the sidebar's). Also swept every remaining user-facing "DocType" string (button/prompts/stat tile/table headers/placeholder) to "Record Type" - the original `e2549ad` rename only changed the screen title, not the strings around it; internal identifiers (`currentDoctype`, `renderDocTypeBuilderView`, etc.) deliberately left alone. Found `index.html`'s `#register-doctype-modal` is dead markup (nothing in `app.js` calls it) still saying "DocType" - flagged, not touched, since deleting dead code is a separate decision. `app.js?v=` cache-buster already at `16` from the concurrent session's own edits in the same file, so no further bump needed.
    - **"Stores" master fixed - three compounding bugs (2026-07-20, live user report)**: `Stores` was the only doctype in the whole system with zero fields; also had zero `role_permissions` rows (default-deny 403 for every role including HR/Admin) and the sidebar's own "Stores" nav item pointed at doctype name `'Store'` (singular) while the registered one is `'Stores'` (plural) - a regression typo, confirmed against an archived patch script that had it right. Fixed all three via `db/migrations_stores_master_fields.sql` (applied live to `custom_erp`) plus a one-line `app.js` fix. `Location` already covers the same "Store" concept via its `type` field (`Store,Warehouse,HO`) - flagged as likely pre-`Location` leftover duplication, not merged/deprecated (a separate decision from making it work). Full detail: `docs/project_ledger.md` §21.
    - **Stage 21 (2026-07-20, built + live-verified, run concurrently with the Stage 20 planning session above).** Per two screenshotted bugs (Finance/GL table clipping with no scroll; DocType Builder's "Enter DocType Name" still a raw OS prompt) plus the user's own follow-up choices on scope. `.table-panel` (styles.css) was `overflow: hidden` in a flex layout that already sized it correctly — one CSS-line fix to `overflow: auto` made the pre-existing sticky-`th` rule work app-wide with no per-page JS. Fixed 2 more live CSS bugs found in the same pass (unstyled KPI numbers from a `.stat-value`/`stat-val` class-name mismatch; dead `.stat-icon`/`.card-icon` rules). Migrated the last 4 raw `window.prompt()` calls to the app's existing custom dialog. Per the user's explicit choice, removed all 26 sidebar per-item icons for an icon-minimal SAP/MS-style nav (3 were exact-duplicate paths on unrelated concepts). Replaced the static sidebar-footer logout button with a clickable account-menu popover (name/email/role, My Profile, Sign Out) plus a brief sign-out transition. New self-service **User Profile** screen (`internal/server/handlers_profile.go`: `GET/PUT /api/v1/me`, `POST /api/v1/me/change-password`; new additive `users.idle_timeout_minutes` column via `db/migrations_stage21_user_profile.sql`, applied live to `custom_erp`; a first-ever `Employee.user_id → user` reverse lookup) driving a new client-side idle-timeout auto-logout timer. **Numbered Stage 21, not 20**, specifically because this stage's own in-progress "Stage 20" code comments collided with the concurrent session's Stage 20 backlog being drafted in the same files at the same time — caught via `git status` mid-session and renumbered before anything shipped; see the concurrent-sessions note below, this is a live example of it, not just a hypothetical. Added `CLAUDE.md`'s new "First principle: lightweight, future-proof, solid" section per the user's own mid-session request. Also fixed live mid-session, per the user's own follow-up screenshot: PIM's 6 doctype-backed tabs (Product Families, Attribute Definitions, etc.) hard-navigated via `renderView('doctype-table')`, discarding the whole PIM title/tab-bar shell - extracted a reusable `renderPIMShellHeader()` that `renderDocTableView()` now prepends whenever the current doctype is PIM-owned. Full detail: `docs/project_ledger.md` §17, `docs/micro_checklist.md` Stage 21 (21.9-21.11 are queued follow-ups the user explicitly asked to hold for a later session, not started; 21.8, the PIM shell fix just described, was requested and closed later the same session).
    - **Stage 22 (2026-07-20, built + live-verified, same session as Stage 21 above — this is that session's continuation, not a third concurrent one).** Per explicit follow-up request: test every page and judge whether it genuinely works, literally walk through the guide docs' own steps for a real task, fix what's broken, correct the docs — codified as `CLAUDE.md`'s new "Second principle: complete, smooth, and documented end-to-end." A Playwright sweep (isolated additive HR/Admin test identity, MFA-enrolled through the real API flow, not by reading any secret from the DB) hit all 60 reachable pages against a throwaway port with zero console/5xx errors - but found **Inventory, Transfers, Users, and Roles were all dead "Module Setup Pending" mocks** (pre-existing, not a Stage 21 regression), and that neither guide doc actually documented "how do I add a master record" despite the mechanism itself working smoothly. Built real versions of all four screens (Inventory reuses the existing current-stock report endpoint plus a real search box; Transfers is a new bespoke view driving `engines/transfer_orders.go`'s already-built but previously frontend-less dispatch/receive lifecycle; Users/Roles are new HR/Admin-only endpoints in new `internal/server/handlers_admin_identity.go`, since `tenant_default.users`/`role_permissions` are raw SQL tables the generic doctype engine can't reach) - full item-by-item detail and the two more bugs found live (a JSONB-replace-not-merge footgun in the generic update path; a raw Postgres error leaking to the UI) are in `docs/micro_checklist.md` Stage 22. `USER_GUIDE.md`/`ADMIN_GUIDE.md` updated with the walkthroughs that never existed plus an honest correction (the sidebar isn't actually role-filtered yet - flagged as Stage 22.6, not fixed this pass, since it's a separate cross-cutting feature). All QA test data removed afterward. Full detail: `docs/project_ledger.md` §19.
    - **Stage 23 (2026-07-20, built + live-verified, run concurrently with the "Finance Maturity" session's own uncommitted work in the same tree).** User supplied `ERP_Standard_Message_Control_Matrix_Final.xlsx` (301 rows standardizing every user-facing message across every module) and asked whether it already existed in the app, and to build/fix it if not. It didn't - errors were ad hoc, plain-text `http.Error(...)` mixed with hand-rolled JSON across 11 files, and `apiMiddleware`'s `Content-Type: application/json` header meant every plain-text error body silently broke the frontend's `await res.json()` parsing (a real live bug, not just an inconsistency). Generated `internal/server/error_catalog_generated.go` (302 codes, via new `scripts/gen_error_catalog.py`) as the single source of truth; built `internal/server/apierror.go`'s `writeAPIError`/`writeAPIErrorGeneric` as the one place every error response is written from; wired all framework-level paths (`middleware.go`: panic, rate limit, auth, module/feature gates) plus all 400 existing error call sites across all 10 `handlers_*.go` files (delegated to parallel background agents, one per file/group - deliberately excluded `handlers_finance_maturity.go`, the concurrent session's in-progress file). New frontend `showToast()`/`renderPageBanner()` primitives for the two display styles that had no existing UI; `apiFetch`/`apiUpload` now read the real error/code from the response body instead of a hardcoded string. **Real structural finding**: the catalog has zero rows at HTTP 400 (uses 422 uniformly) and zero at 405, while much of the existing codebase uses those statuses - flagged as a `[needs product decision]` convention gap rather than silently changing either side. `go build`/`go vet`/`go test ./... -p 1` clean (one stale test assertion updated - intended message-text change, not a regression); manually verified against a live throwaway instance (port 8101). Full detail: `docs/project_ledger.md` §22, `docs/micro_checklist.md` Stage 23, `docs/specs/message_catalog.md` (new canonical reference).
    - **Stage 24 (2026-07-20, planning only at the time — see the 2026-07-22 audit entry below, which found it was actually already built).** User supplied `docs/ERP_LOOPHOLES_ANALYSIS.md` (an AI code review: 34 numbered security/data-integrity/concurrency/architecture findings) and asked for a plan. Every finding was re-verified against live source (grep/Read, not taken on faith) before being turned into a checklist item: 4 turned out already fixed by later stages the source doc predates (MFA rate limiting, soft-delete status validation, upload content-type sniffing, CSRF non-applicability — all audited `[x]` with the current code cited, no rebuild), and 2 (`handleLabels`/`handleSequence`/`handlePrefix` having zero role check at all, and `handleSwitchIndustry` likewise plus an unsanitized file-path build from client input) turned out worse than the source doc described. Broken into 32 items (24.1-24.32) in `docs/micro_checklist.md` Stage 24, grouped Critical access-control → Critical/High data-integrity/concurrency → Operational hardening → Defense-in-depth → deliberately-deferred/needs-product-decision, each scoped to avoid new Go dependencies per this repo's lightweight-first principle (no `golang-jwt`, no `shopspring/decimal`, no `gobreaker` — stdlib equivalents instead).
    - **Stage 20 Track B.3 (2026-07-20, built + live-verified, the "Finance Maturity" session referenced by Stage 23 above - now complete).** User asked to continue Stage 20 with Finance Maturity (Track B.3, 20.25-20.34). New `BankAccount`/`BankStatementLine`/`PaymentProposal`/`TDSSection`/`DebitNote`/`CreditNote` doctypes (`db/migrations_stage20c_finance_maturity.sql`) plus new engines `bank_reconciliation.go` (matches imported statement lines against `gl_postings` via a new additive `matched_statement_line_id` column), `payment_proposal.go` (batches `Matched` vendor invoices through the existing `PayVendorInvoice`), `tds.go` (TDS-aware payment as a deliberate sibling of `PayVendorInvoice`, not a branch inside it), `notes.go` (debit/credit note GL reversal via two new contra accounts). **The interesting find**: `SalesInvoice` had existed since Stage 1 as a registered doctype with zero amount field/GL posting/frontend - a dormant shell. New `engines/sales_invoice.go` wires it up for real (Post books AR, Settle clears it), which is what gives the new `GetReceivablesAgeingReport` a genuine receivable to age, symmetric to the existing Payables Ageing. Also closed two frontend gaps found along the way that blocked testing the above through the UI at all: VendorInvoice (Stage 17.8) and Accounting Periods (Stage 17.4) both had zero screens despite working APIs. 5 new Finance-module screens, 2 new Reports tabs, 1 new Finance/GL tab. **A concurrent-session hiccup handled mid-session**: a live-verification build attempt hit a transient compile error in `handlers_admin_identity.go`/`middleware.go` from another session's in-flight edit; waited, rebuilt, confirmed it self-resolved without touching those files. `go build`/`go vet`/`go test ./... -p 1` clean; `node --check public/app.js` clean; live-verified end-to-end against a throwaway port-8099 instance logged in as `manager1` (TDS math/GL split, debit/credit note postings, sales-invoice post→settle reflected in Receivables Ageing, a payment proposal executed via the real pay path, a bank statement line reconciled against a real GL posting, and a period-close checklist correctly surfacing real pending-approval state) - all throwaway test data removed afterward. 20.30/20.31 (e-invoice/IRN, e-way bill) remain blocked on real GSP credentials, unchanged. Full detail: `docs/project_ledger.md` §23, `docs/micro_checklist.md` Stage 20 Track B.3.
    - **Stage 20 Track B.4 (2026-07-20, built + live-verified, immediately after Track B.3 above - Stage 20 is now fully closed except Track A and 20.30/20.31).** User asked to continue with Stage 20's remaining open items. New `engines/report_registry.go` (a `ReportDefinition` framework - columns/params/a `Run` function/an optional `DrillDown`, registered via `RegisterReport`; deliberately not a full ad hoc SQL builder, an injection risk this ERP's scope doesn't need) + `engines/report_definitions.go` (all 6 pre-existing reports wrapped as-is, plus 7 new catalog additions: GRN Register, Cash Book, Bank Book, Asset Register, Loyalty Ledger Summary, Production Order Status, RFQ Comparison) + `engines/report_export.go` (async export reusing the exact `StartOutboxWorker` ticker shape, CSV stored directly in the job's own JSONB document like Stage 15.2's `ImportJob.error_csv`). Column-level masking redacts Sensitive-flagged columns to `•••` for any role below Store Manager/HR-Admin. Drill-down re-derives each summary report's own bucket-boundary logic rather than a second copy of it. **Two Stage 20.40 catalog candidates deliberately skipped, not silently deferred**: Attendance Summary (no data model exists anywhere in this codebase) and Stock Ledger (`StockLedgerEntry`/`WriteStockLedgerEntry` exist but are genuinely dead code, never called from any real flow - wiring that up touches 5+ already-verified flows, too large for this pass). New "Report Catalog" Reports tab, entirely metadata-driven from the catalog endpoint; its background-export download uses an authenticated `apiFetch` + Blob (a plain `<a href>` can't carry the required Bearer token - the pre-existing PIM import-errors download link uses that broken pattern, a latent bug not touched this pass). **A user request addressed inline mid-build**: "Fix doctypes. keyword from entire erp... removed this terminology from ui and docs" - found a concurrent session (§21's 21.12) had already swept almost everything to "Record Type" and explicitly flagged one gap (`index.html`'s dead `#register-doctype-modal`, zero JS references); deleted it, plus swept `ADMIN_GUIDE.md`/`README.md`/`UAT_CHECKLIST.md` (not covered by 21.12), deliberately leaving the deep-technical `framework_architecture.md` alone (added a scoping note instead - it explicitly compares to ERPNext/Frappe's own DocType concept and describes the real `doctype_meta`/`doctype_fields` tables). `go build`/`go vet`/`go test ./... -p 1` clean; `node --check public/app.js` clean; live-verified end-to-end against a throwaway port-8099 instance as `manager1` (catalog listing, ran 7 different reports including every new catalog addition, a required-param 400, two drill-downs returning exactly the right underlying rows, a saved-filter create+list, and the full export queue→worker-completes→download cycle) - all throwaway test data removed afterward. Full detail: `docs/project_ledger.md` §24, `docs/micro_checklist.md` Stage 20 Track B.4.
    - **Stage 25 (2026-07-21, built + live-verified, Batches 1-2 of a multi-batch effort, run concurrently with the Stage 23 follow-up session below).** Asked to actually build out Stage 23's ~187 unwired "Mature ERP" catalog codes rather than leave them backlogged. Recount against live source found only 7 of 187 wired and the rest spread almost entirely across already-built modules (Master Data, Manufacturing, HR, GRN, PO, Sales, etc.), not the unbuilt-subsystem framing the original doc used — full breakdown in `docs/micro_checklist.md` Stage 25. **Batch 1**: `engines/doctype.go`'s `ValidateDocument` (the one generic validation path every doctype's create/update already runs through) gained a `ValidationError{Code, SubFor, Message}` type so its 4 existing scenarios (mandatory/format/select/link) attach precise codes (`GLOBAL-0001`/`0002`, `META-0198`/`0199`) from one shared change instead of a per-doctype sweep; same pattern reused for upload-size (`GLOBAL-0007`) and file-type (`GLOBAL-0008`) rejections. Added a real new check `CreateAccountingPeriod` was missing (start-after-end dates → `GLOBAL-0012`). **Batch 2**: new `engines/master_data_validation.go` — 6 real Master Data checks with actual fields to validate (Item HSN format/required-when-taxable, duplicate barcode; Vendor GSTIN/bank-account format; Customer mobile format) → `MASTER-0042/0043/0049/0051/0052/0053`. The other 9 Master Data codes and the inactive-item-transaction check were deliberately left — either the field doesn't exist on the doctype yet (needs a metadata migration, a product call) or the fix would be a global behavior change (`verifyDocumentExists`, used by every Link field app-wide) whose semantics need a per-doctype audit first. Batches 3+ (Manufacturing/HR/GRN/PO/Sales/etc., Channel Connectors/Omnichannel, POS Cash Drawer/Extension Hooks, and the genuinely-new-API-surface items — deployment/backup status reporting, SaaS subscription limits) fully itemized in `micro_checklist.md` Stage 25, not started. `go build`/`go vet` clean; `go test ./engines/... ./internal/server/... -p 1` clean except the same pre-existing `TestEngines/FinanceDoubleEntryAndPOS` regression noted below. Live-verified against a throwaway port-8123 instance on the real `custom_erp` DB — minted a throwaway HR/Admin JWT directly via `engines.SignToken` rather than fighting through admin MFA (no real user/session touched), confirmed 9 of the new/reclassified codes fire correctly with right message text and correct `{field name}` substitution through the real HTTP path; a valid-GSTIN Vendor create correctly passed validation and only failed at the DB insert on the synthetic user's expected FK constraint. No test data persisted; scratch binary/logs removed, server stopped afterward. **Found this session running concurrently with the Stage 23 follow-up session below** (discovered mid-session when `handlers_finance_maturity.go` — a file this session was about to sweep — turned out already swept by the other session); reconciled by re-checking `git status`/diffs before each subsequent edit instead of trusting a stale view of the tree. Full detail: `docs/project_ledger.md` §26, `docs/micro_checklist.md` Stage 25.
    - **Stage 23 follow-up (2026-07-21, built + live-verified, on top of `99d63de`).** Stage 23 (§ above) closed with two open product decisions and four backlog items. This session made both decisions and built what they unblocked, prompted by the user handing back exactly that punch list and asking to build it. **Decided: standardize the codebase on 422** (not a 400 variant added to the matrix) — `apierror.go`'s own generic-fallback map already treated 400/422 as the same scenario, and the frontend has zero hardcoded checks on status 400, so this was safe to do mechanically: 186 occurrences of `http.StatusBadRequest` → `http.StatusUnprocessableEntity` across all 10 original handler files (background-agent sweep, `go build`/`vet`/`test ./... -p 1` verified) plus `handlers_finance_maturity.go`'s own 13 (see next). **Decided: keep `featureGate`'s fail-closed 403**, not the matrix's soft-200 for `SAAS-0192` — a disabled-feature response should never look like success to a caller that only checks for 2xx; no behavior changed, just the comment/docs updated to record it as resolved. **23.9**: `handlers_finance_maturity.go` (the concurrent session's work, landed as part of `99d63de`) swept onto `writeAPIErrorGeneric` + 422, matching every sibling file. **23.8**: rather than migrating ~70 screens by hand, found the actual single choke point (`showApiError()` in `app.js`, already used by ~32 call sites) and made it dispatch on a new `display_style` field now threaded through the JSON error envelope from the catalog — `"Toast"`/`"Page banner"` render via the existing (previously unused-by-default) `showToast()`/`renderPageBanner()` primitives, everything else keeps the modal fallback. Also converted ~19 call sites hand-duplicating that same parsing logic to call `showApiError` directly. **A pre-existing, unrelated regression found while verifying, not fixed here**: `TestEngines/FinanceDoubleEntryAndPOS` fails a trial-balance assertion (9500 vs expected 9000) — confirmed via `git stash` this already fails on `99d63de` with zero other changes, so it predates this session; flagged under Stage 20 Track B.4 in `micro_checklist.md`, not investigated/fixed (out of scope, and someone should read that Stage's double-entry code carefully rather than guess at a fix). Live-verified against a throwaway port-8102 instance: malformed JSON to `/api/v1/login` now returns a real `422`/`GLOBAL-0002` with `display_style` present in the body (previously would have been `400`) — confirms the conversion and the new envelope field both work on the running binary. Full detail: `docs/project_ledger.md` §25, `docs/micro_checklist.md` Stage 23 follow-up, `docs/specs/message_catalog.md` (updated).
    - **Documentation sync + first-principle check (2026-07-21, no feature code changed).** Asked to bring all three tracked docs current and check the project against `CLAUDE.md`'s "lightweight, future-proof, solid" first principle. Found the doc drift was mostly bookkeeping, not content: this file's own §6 "Uncommitted files"/commit-hash notes still described the Stage 23 follow-up work as uncommitted after it (and Stage 25 Batches 1-2) had actually landed as `e5a3456`/`4802346` — corrected here. `project_ledger.md` §11 had an unmaintained commit pointer stuck at 2026-07-17 (Stage 13 era) — bumped and annotated that this file's §6 is the actively-maintained one. `micro_checklist.md` 17.9's checkbox was still `[ ]` despite being shipped since 2026-07-19 (already flagged in-file, just never actioned) — flipped to `[x]`. **First-principle adherence checked against real history, not assumed**: `go.mod`'s `require` block has held exactly `lib/pq`/`golang.org/x/crypto` since Phase 2-5 (2026-07-11) through Stage 25 — zero new dependencies across 25 stages, confirmed via `git log -p -- go.mod`; every `db/migrations_stage*.sql` file grepped for `DROP TABLE`/`DROP COLUMN`/`ALTER COLUMN ... TYPE` returned nothing, confirming the additive-only rule has held through Stage 24. Used that history to refine (not just restate) the "reuse existing patterns" bullet in `CLAUDE.md` with the specific reusable frameworks Stage 20-25 actually established (maker-checker approval reuse, `BulkImportCSV` reuse, the `ReportDefinition` registry, the `writeAPIError` envelope) plus a new explicit callout of the "one shared choke point" technique Stage 23/25 used repeatedly (`ValidateDocument`, `apierror.go`, `showApiError()`) instead of per-call-site sweeps.
    - **Stage 25 Batch 3 (2026-07-22, built + live-verified, ran concurrently with the Stage 24 audit session referenced below — that session's own note about this one being "mid-flight" is now stale, superseded by this entry).** User asked to start Batch 3 (Manufacturing/HR-Payroll/GRN/Purchase Order+RTV/Sales-POS+Sales Return/Stock Transfer/Vendor Invoice/WMS/Fixed Assets/Expense — 75 catalog codes). Full per-code audit against live source (not a blind sweep, same discipline as Batch 2) found a much lower real-hit rate than Batch 1-2's precision-pass modules: 29 of 75 wired to real checks, 46 deliberately deferred — several of these modules are genuinely thin MVP engines by original design (`engines/manufacturing.go`'s own header comment already scopes out routing/QC/costing), and Purchase Return/RTV (`PURCHA-0116..0120`, all 5 codes) turned out to reference a doctype that was never actually built at all (confirmed via a `doctype_meta` grep across every migration file — only an unrelated same-named `bin_stock` condition label exists). New `engines/transactional_validation.go` (`ValidateTransactionalRules`, same choke-point pattern as Batch 2's `ValidateMasterDataRules`, wired into `handlers_core_doc_engine.go`'s generic doc POST path for `GRN`/`PurchaseOrder`/`TransferOrder`/`ProductionOrder`/`Employee`/`Leave`) plus reclassifications/new checks spread across `engines/approval.go` (new `ErrApprovalRoleMismatch` sentinel), `assets.go`, `fulfillment.go` (`ProcessReturnAnywhere` previously took an unverified free-text order ID with zero cross-check against any real sale — genuinely open before this pass, now resolves against a real `POSCart`/`SalesInvoice`), `manufacturing.go`, `tds.go`, `transfer_orders.go`, `vendor_invoice.go`. New `writeEngineError` helper (`apierror.go`) generalizes the `*ValidationError` type-assert dance to the action-style handlers. Two real gaps caught and fixed *during* live verification rather than shipped broken: `GOODSR-0095` was fully designed during the audit but the code was never actually written until live-testing exposed the hole; a GRN-received-quantity aggregate query didn't exclude `Cancelled` GRNs, which would have let a cancelled GRN's quantities keep counting against the PO-fully-received checks indefinitely. `go build`/`go vet` clean; `go test ./engines/... ./internal/server/... -p 1` clean except the same pre-existing `TestEngines/FinanceDoubleEntryAndPOS` regression (re-confirmed via `git stash`); two existing tests needed real updates for genuine behavior changes (`engines_test.go`'s Return Anywhere subtest now seeds a real originating sale; `transfer_orders_test.go`'s short-receive subtest now supplies a shortage reason, plus a new subtest for the no-reason-rejected case). Live-verified against a throwaway port-8123/8124 instance on the real `custom_erp` DB, this time using real seeded user identities (`system`/`cashier1`) rather than a synthetic one so writes persist far enough to exercise cross-document checks — 28 of the 29 wired codes confirmed firing through the real HTTP path with correct code + message (full list in `micro_checklist.md` Stage 25.4's verification note); all test data removed, scratch `cmd/minttoken_scratch25b3` tool/binaries/server processes removed afterward. Full detail: `docs/project_ledger.md` §28, `docs/micro_checklist.md` Stage 25.4.
    - **Stage 24 audit (2026-07-22): asked to start building it, found it was already built.** Before writing code, read the actual current source for each of the 20 build items and found all 24 (24.1-24.24) already implemented and committed in `99d63de` — the same commit whose message says "...+ concurrent Stage 23/24 work"; `micro_checklist.md`'s Stage 24 section had simply never been flipped from its `[ ]` planning-draft state to match, the same stale-checklist pattern as Stage 12.1/17.9 before it. Re-verified every item individually against live source rather than trusting the commit message (see `micro_checklist.md` Stage 24 for the per-item citations) and live-tested the highest-risk ones end-to-end against a throwaway port-8099 instance on the real `custom_erp` DB: 24.1's token `loc` claim (decoded a fresh `manager1` token), 24.2/24.3's role gates + path-traversal allowlist (403s for a Store Manager, a 422 rejection of `../../` payloads for HR/Admin, a real reload for a valid code), 24.10's optimistic lock (a stale `expected_version` got a real 409, a correct one advanced the version, verified via re-fetch), 24.14's health endpoint, 24.16's debug-panic gate (reachable with `ENV` unset, a genuine 404 with `ENV=production`), and 24.19's `iat`/`jti` claims. 24.5 and 24.11 (idempotency dedup, vendor-invoice-override-through-approval) are already proven by existing passing subtests (`engines_test.go`, `vendor_invoice_test.go`) rather than re-derived live. `go build`/`go vet` clean; `go test ./... -p 1` clean except the same pre-existing `TestEngines/FinanceDoubleEntryAndPOS` regression (re-confirmed via `git stash` to fail identically on committed `HEAD`, unrelated). One gap found: 24.18's json-unmarshal-error sweep left 1 production-code occurrence (`handlers_core_doc_engine.go:427`) — deliberately not touched since that file is being actively edited right now by a concurrent Stage 25 Batch 3 session and the edge case already degrades safely. At the time, only 24.27-24.32 (needs-decision/deliberately-deferred items) remained genuinely open — see the next entry below, they were also built the same day. No test data left behind (one `Location`/`HO` edit made and reverted); throwaway instance/binary/scratch token-minting tool all removed. Full detail: `docs/project_ledger.md` §27, `docs/micro_checklist.md` Stage 24.
    - **Stage 24, items 24.27-24.32 (2026-07-22, same session as the audit above, built on explicit user request overriding their own "needs decision"/"deferred" framing).** Each item's own original reasoning was read first (see `micro_checklist.md`'s per-item notes), then decided/scoped down where it said `[needs decision]`. **24.27**: rather than making `db/migration.sql` generate its own random credential (would need a new `pgcrypto` Postgres extension dependency), `engines.EnforceNoDefaultAdminCredentialInProduction()` checks the seed admin hash at startup (`routes.go`'s `Run()`) and refuses to boot when `ENV=production` and it's still active — live-verified both branches by rebuilding with `ENV=production` (real refusal, exit 1) and unset (normal start). **24.28**: new `engines/password_reset.go` — a reset token's SHA-256 hash is stored, never the raw value; stdlib `net/smtp` sends the email if `SMTP_HOST` is configured, otherwise logs the link locally (same safe-no-op posture as `SendOpsAlert`/`OPS_ALERT_WEBHOOK_URL`); new public `POST /api/v1/auth/forgot-password`/`reset-password`, both in the existing 5/min `"login"` rate-limit bucket; `handleForgotPassword` responds identically regardless of whether the account exists (no enumeration oracle) — live-verified the full cycle against `manager1` (identical fake-vs-real responses, old password failing post-reset, new password working, a reused token rejected), password reverted after. **24.29**: `engines/connector_http.go`'s `doConnectorRequest` (the one choke point every Shopify/BigCommerce/Magento call goes through) gained a small stdlib-only circuit breaker keyed per platform — 5 consecutive failures opens it for 30s, then one half-open trial; proven by a new `TestConnectorCircuitBreaker` against an `httptest.Server` (no live connector to actually fail against). **24.30**: rather than a real per-tenant DB pool (a bigger architecture change than this app's single-shared-pool scale justifies), `apiMiddleware` now caps each tenant to 15 concurrent in-flight requests, fast-`503`ing the rest — verified by build/test and this session's own live traffic passing through cleanly, not by forcing the actual 503 (not worth synthesizing for this low-severity item). **24.31**: a new `doctype_fields.max_length` column plus a blanket 10,000-char default, wired into `ValidateDocument` — every doctype covered by one change; live-verified a 10,001-char rejection and a normal value passing through. **24.32**: `BulkImportCSV` now batches 500 rows per transaction instead of one transaction for the whole file, bounding worst-case lock-hold time — a deliberate side effect noted, not hidden: a large real import can now partially persist if a later batch fails, instead of all-or-nothing. New migration `db/migrations_stage24b_deferred_hardening.sql` applied live to `custom_erp`. `go build`/`go vet` clean; `go test ./... -p 1` clean except the same pre-existing `FinanceDoubleEntryAndPOS` regression plus **one newly-found, unrelated, not-fixed-here issue**: `TestEngines/DocTypeValidationAndAuth` now fails because the shared dev DB's `Brand` doctype has accumulated mandatory fields (`fefo_enabled`, `wafer_batch_id`, `silicon_thickness`) from earlier industry-profile-switching sessions, which the test's hardcoded payload predates — confirmed as DB-state drift, not a code regression (the mandatory-check code path is untouched, and 24.31's own new length check is a separate code path from the "field is required" error the test hits). Hit one transient compile error mid-session from a different concurrent session's in-flight edit to `engines/inventory.go` (`PostInventoryLedger`'s signature, for Stage 20.13 offline POS sync) — waited, rebuilt, confirmed it self-resolved without touching those files. Full detail: `docs/project_ledger.md` §30, `docs/micro_checklist.md` Stage 24's "Needs a product/ops decision, or deliberately deferred" section.
    - **Stage 21.9 (2026-07-22, built + live-verified, started after the user asked to begin Stage 17/20/21's remaining open items).** Most of those items are still blocked on user-supplied external input (contacts, webhook URL, connector/GSP credentials, hosting decision, security reviewer — confirmed still unavailable this session) or needed a product decision first; 21.9 (full step-by-step SOP docs) had neither blocker, so it was built first. New `docs/guides/USER_SOP.md` (437 lines)/`ADMIN_SOP.md` (233 lines) — literal click-by-click procedures for every sidebar screen and every maker-checker workflow, grounded in a full read of `public/app.js`'s render functions, cross-linked from both existing guides and `docs/README.md`. **This pass surfaced a real gap, fixed rather than just documented**: every generic record-list screen (Vendors, Stores, Bin Master, Master Definition lists, etc.) had a Delete action but no Edit action at all — `renderDocTable()` only ever rendered a trash-can button, contradicting `USER_GUIDE.md` §8's existing claim. The backend's generic `/api/v1/doc/{doctype}/{id}` update route already fully supported this (RBAC + validation + optimistic locking via `expected_version`) with zero frontend caller — fixed as a frontend-only change: an Edit button per row, `openDynamicModal()` now accepts an existing record to prefill from, `handleDynamicFormSubmit` POSTs to `/{id}` with `expected_version` when editing. Live-verified against a throwaway port-8099 instance (`manager1`, real `custom_erp`): a disposable `Bin` record's `capacity` edited 10→25 with `expected_version:1` persisted and bumped `version` to 2; replaying the stale `expected_version:1` correctly `409`'d. Other real gaps found and left honestly documented, not fixed (separate scope decisions): no GRN-creation screen anywhere despite Vendor Invoice matching depending on one, `PurchaseRequisition` has no sidebar entry, WMS bin-level ops (putaway/pick-list/condition-transition/cycle-count) are API-only, Approval Rules config has no admin screen, Vendor Invoice override-pay has no frontend control. `node --check public/app.js` clean; no Go files touched. Full detail: `docs/project_ledger.md` §29, `docs/micro_checklist.md` Stage 21.9.
    - **Stage 20.13 + 21.11 (2026-07-22, built + live-verified, same session as Stage 21.9 above, continuing once those two decisions were asked for and answered).** **20.13 decisions (user)**: offline window = one shift, tied to the cashier's own open POSSession (closing a session with sales still unsynced isn't allowed, so this is the natural bound rather than a separate timer); conflict behavior = an offline sale always posts once synced (goods already left the store) and is allowed to push stock negative, flagged for manager review. Built: `public/app.js`'s `localStorage`-backed offline queue (`checkoutOnlineOrQueue`/`trySyncOfflineQueue`, wired into `submitPOSCheckout` + a new `setupOfflineSync()` in `init()`) queues a sale instead of showing `apiFetch`'s blocking error whenever offline, replays in order on reconnect (stopping at the first still-unreachable entry, dropping/surfacing any the server genuinely rejects), and reuses the cart's already-client-generated `cart_number` as the idempotency key `handleCheckout` already enforces server-side — no new dedup mechanism needed. `closePOSSessionFlow()` now refuses to close while the queue is non-empty (client-side only, the server can't see an unsynced queue) — **also fixed in the same touch**: that function's success alert referenced an undefined `data` variable (response body was never parsed), a pre-existing bug that would have thrown on every successful close. Server-side: `PostInventoryLedger` (`engines/inventory.go`) gained an additive `allowNegative` bool (every existing caller passes `false`, unchanged); `FinalizePOSCheckout` reads a new `offline_synced` flag back off the cart's own stored data rather than as a parameter, so `handleDecideApproval`'s separate discount-approval finalize path gets the same behavior for free (verified live). Any shortfall is recorded as a new `POSOfflineSyncVariance` document (new migration, `POSSession`-style "engine writes directly" pattern), reachable via a new "Offline Sync Review" sidebar entry and reviewable through Stage 21.9's new Edit path. **21.11 decision (user)**: Odoo/ERPNext-style naming, scoped to the ~12 top-level sidebar module labels only (not individual screen names). Renamed in `index.html`: POS→Point of Sale, Finance→Accounting, Fulfillment & Marketplace→Sales & Marketplace, Procurement→Buying, Inventory & WMS→Stock, Master Definition→Setup, Admin & Settings→Settings; left unchanged with reasoning: HR & Assets, Manufacturing, PIM, Reports, Dashboard. Every stale reference swept (3 `app.js` page-text mentions, `USER_GUIDE.md`/`USER_SOP.md`/`ADMIN_SOP.md`/`UAT_CHECKLIST.md`); two historical/deep-technical docs left alone per this repo's existing precedent. **Live-verified**: 20.13 against a throwaway port-8097 instance (`manager1` + a scratch-minted HR/Admin token, deleted after use) — normal checkout still rejects insufficient stock (no regression), the same request with `offline_synced:true` posts and drives stock negative with a variance record created, a duplicate-`cart_number` replay doesn't double-post, and a discount-gated offline cart correctly routes through approval first with the same negative-stock behavior applied on approve. 21.11 verified against a throwaway port-8098 instance by fetching the served `index.html` and confirming every label. All test data removed; both throwaway instances + the scratch token tool stopped/removed. `go build`/`go vet`/`node --check public/app.js` clean. **Pre-existing, unrelated test failures noted, not fixed** (the long-standing `FinanceDoubleEntryAndPOS` regression, and a `DocTypeValidationAndAuth`/Brand-industry-profile-pollution failure the concurrent Stage 24.27-24.32 session below independently found the same session too): see `docs/micro_checklist.md` Stage 20.13's own note. Full detail: `docs/project_ledger.md` §31, `docs/micro_checklist.md` Stage 20.13 and 21.11.
*   **Dev Server State**: Every build in this session (Stage 20 Track B.1/B.2, the concurrent Stage 21/22 work, and the nav redesign) used its own separate throwaway instance for verification (ports 8097/8098/8099, each stopped again afterward) so as not to disturb the user's live port-8080 session mid-build. **Once all of the above was ready and live-verified, port 8080 was deliberately rebuilt and restarted** (`go build -o custom_erp.exe ./cmd/server`, run from the repo root so its relative `./public` static-file path resolves, `PORT=8080`/`DATABASE_URL` pointing at `custom_erp` on 5435, output redirected to the gitignored `logs/erp-server.{out,err}.log`) - the user was actively looking at the old stale binary (screenshotted "Failed to load users" and old "DocType Builder" text) and needed it current, not just left alone. As of this restart, port 8080 is running a binary built from every change described in this section, confirmed via `GET /api/v1/version` and a spot-check that a new route (`/api/v1/pos/session/current`) returns `401` (route exists, needs auth) rather than `404`. Confirm with `netstat -ano | findstr "5435 8080"` and `tasklist /FI "IMAGENAME eq custom_erp.exe"` before assuming state in a future session - if the binary predates this note's own timestamp, it's stale again and should be rebuilt the same way.
*   **Port 8080/5435 observed to restart mid-session, 2026-07-20 (Stage 20 Track B.3 session)**: partway through this session both went from running to stopped to running again under new PIDs, with a live client connection observed once back up - someone (the user or another process) restarted their own dev stack independently; this session did not stop or start port 8080 itself, and used its own throwaway 8099 instance for verification as usual (stopped again afterward). The current port-8080 binary's freshness relative to Stage 20 Track B.3's new routes (`/api/v1/finance/bank-reconcile`, `/api/v1/finance/payment-proposal*`, `/api/v1/finance/sales-invoice/*`, `/api/v1/finance/debit-note/*`, `/api/v1/finance/credit-note/*`, `/api/v1/procurement/vendor-invoice/pay-with-tds`, `/api/v1/reports/receivables-ageing`, `/api/v1/reports/gst-return-summary`) is **unknown** - check `GET /api/v1/version`'s build time against this file's edit time before assuming the live session has these; rebuild/restart if stale, same recipe as above (Postgres itself: `& "$env:USERPROFILE\pg-portable\pgsql\bin\pg_ctl.exe" start -D "$env:USERPROFILE\pg-data" -w`, avoids the documented `manage.ps1 start`/`pg_ctl -w` hang-in-sandbox issue).
*   **Verification Status (superseded, kept for history)**: Stage 22's own verification (Transfers/Users/Roles screens, throwaway port-8099) — see below for the current state.
*   `internal/server/server_test.go`'s integration test opens a POS session (`engines.OpenPOSSession`) before checkout, a hard precondition since Stage 20 Track B.1. Note (found 2026-07-19, still true): every test file's `db.InitDB` call uses a literal hardcoded connection string to the real `custom_erp` database on port 5435, not an isolated test DB - `DATABASE_URL` has no effect on `go test`. When landing a new migration, apply it to `custom_erp`, not just `custom_erp_test` (that one's only used by the Stage 17.3 backup/restore drill).
*   **Stage 25 Batch 3 is now committed** as `a6f1a93` ("Stage 25 Batch 3: wire Manufacturing/HR/GRN/PO/Sales/Transfer/Vendor/WMS/Assets validation codes") — the "uncommitted as of 2026-07-22" state this note previously described no longer applies; `git log --oneline -5` from `a6f1a93` confirms it's the current tip of `main`.
*   **Uncommitted as of 2026-07-22 (multiple concurrent sessions, on top of `a6f1a93`)**: this session's own files (Stage 24 items 24.27-24.32) are `engines/auth.go`, `connector_http.go`/`connector_shopify.go`/`connector_bigcommerce.go`/`connector_magento.go`, `doctype.go`, `import.go`, new `password_reset.go`, `connector_platforms_test.go`, `internal/server/handlers_auth.go`/`middleware.go`/`routes.go`, and new `db/migrations_stage24b_deferred_hardening.sql` — plus this session's edits to the three tracking docs. **Everything else in the working tree belongs to other concurrent sessions, not this one** — `public/app.js`/`index.html`, `docs/README.md`/`guides/USER_GUIDE.md`/`ADMIN_GUIDE.md`/`UAT_CHECKLIST.md`, new `docs/guides/USER_SOP.md`/`ADMIN_SOP.md` (Stage 21.9 session); `engines/inventory.go`/`pos_checkout.go`/`scale.go`/`manufacturing.go`, `internal/server/handlers_core_doc_engine.go`/`handlers_pim_pos_finance.go`, `engines_test.go`, `server_test.go`, new `db/migrations_stage20_13_offline_pos_sync.sql` (a Stage 20.13 offline-POS-sync session); `engines/saas.go` (an older, still-uncommitted Stage 11.1 fix predating both). **Before committing anything**, run a fresh `git status`/`git diff` and stage only the specific files you actually reviewed as your own — do not assume this note's file list is still exhaustive by the time you read it, this tree has had 3+ sessions landing changes in the same window. Re-confirm with `git log --oneline -5` and `git status --porcelain`.
*   **Local binaries are stale relative to source, no server currently running.** `custom_erp.exe`/`erp-server.exe` at the repo root were both last built 2026-07-20 (~09:30-09:40), before the Stage 23 follow-up and Stage 25 commits (2026-07-21, ~21:24-21:31) - neither reflects the 422 status conversion, the `display_style` envelope field, or the new validation codes. As of this check, no `custom_erp.exe`/`erp-server.exe` process is running (checked via `tasklist`) and nothing is listening on port 8080 - only Postgres (port 5435) is up. Rebuild (`go build -o erp-server.exe ./cmd/server`, run from repo root so the relative `./public` path resolves) and restart before assuming the live app reflects Stage 23 follow-up/Stage 25.
*   **Concurrent sessions**: More than one AI session (and the user directly) has worked on this repo in the same window repeatedly (e.g. a stress-test process, a `DecideApproval` TOCTOU fix, the user committing Stage 16 directly to `main` mid-session on 2026-07-19, and 2026-07-19's `877bdc0` commit of Stage 18 landing mid-build). **2026-07-20**: a Stage 20-vs-Stage 21 numbering collision was caught and resolved (the profile/UI-overhaul session renumbered itself from 20 to 21 once it saw this session's Stage 20 backlog land in the same checklist file), and both sessions' code now sits uncommitted in the same working tree simultaneously - each left the other's files untouched throughout (Stage 20 Track B.1 never touched `handlers_profile.go`/the profile migration/`qrcode.min.js`; verified no overlapping line ranges in the shared frontend files before editing them). **Same day, a third concurrent session** (the "Finance Maturity" work referenced above) ran alongside Stage 23's error-catalog build-out - Stage 23 deliberately excluded `handlers_finance_maturity.go` from its handler sweep rather than risk colliding with that session's in-progress uncommitted work, and re-checked `git status`/`git diff` on the shared `app.js`/`index.html`/`styles.css`/`project_ledger.md`/`ai_handover.md` files at each edit point during the session (all three sessions' changes to those files landed in disjoint regions, confirmed each time before writing). Before staging/committing, always check `git status`/`git diff` and add only the specific files you actually reviewed — never `git add -A` — since another session's in-progress or already-committed work may be sitting in the working tree. **2026-07-22 (Stage 24 audit session)**: found a Stage 25 Batch 3 session actively mid-edit on several engine/handler files while auditing Stage 24 — re-ran `git status` before and after every doc edit, made zero edits to any `.go` file (the one production-code gap found, 24.18's leftover `handlers_core_doc_engine.go:427`, was deliberately left for that reason), and used a separately-named scratch token-minting tool (`cmd/scratch_stage24_minttoken`, deleted after use) rather than touching the other session's own `cmd/minttoken_scratch25b3`. **Documentation convention** (see this repo's own `CLAUDE.md`): keep `docs/micro_checklist.md` (what's to be done), `docs/project_ledger.md` (what's been done, chronological), and this file's §6 (handover snapshot) in sync after every unit of work, proactively.
*   **Known issues & closed roadmap**: `docs/operations/hardening_roadmap.md` (security, correctness, test/CI, release hygiene) is fully closed as of 2026-07-12 — historical record only. Active backlog is `docs/micro_checklist.md`.
*   **Build-completeness vs the original spec PDFs**: `docs/specs/pdf_blueprint_gap_analysis.md` is a 2026-07-12 snapshot — its "Thin" verdict on the business-user-facing layer drove the Stage 13 work above and is now a historical record, not current state. It carries a superseded-status banner pointing back here and to `micro_checklist.md`.

---

## 7. Handover Notes for Incoming AI (Claude / Codex / Gemini)

Welcome! The core system plus a growing set of business modules (POS, Finance/GL, GST, MFA, approvals, HR, Fixed Assets, Expenses, CRM/Loyalty MVP, Manufacturing MVP) are built and operational. Read `docs/micro_checklist.md` (Stage 13) before treating any "[x] completed" checklist item as fully closed — each entry documents its exact scope and any deliberate limitations. `docs/operations/hardening_roadmap.md` (security/correctness hardening) is fully closed and kept only as a historical record. To verify the build or resume development, follow these steps:
1.  **Repository Setup**: Pull latest code from `main` branch.
2.  **Run Database**: Ensure PostgreSQL is running on port `5435`.
3.  **Run Tests**: Execute `go test ./...` to verify all business rules.
4.  **Run Server**: Launch server `./erp-server.exe` or `go run ./cmd/server`.
5.  **Build Assets**: Execute `npm run build` to package frontend minified scripts.
6.  **Access App**: Navigate to `http://localhost:8080` in your browser. You'll land on a login screen — credentials are in `DEV_CREDENTIALS.local.txt` (gitignored, project root; regenerate via a throwaway `golang.org/x/crypto/bcrypt` script if missing, then update `db/migration.sql` and the live `tenant_default.users` table). All native alerts/confirms/prompts have been replaced with a custom-styled Promise-based modal layout as of Stage 21 (2026-07-20) — no raw browser dialogs remain anywhere in `app.js`.
7.  **Handover Ledger**: Reference `docs/project_ledger.md` for historical build logs and chronological records of what was built.

