# Project Progress Ledger: Custom ERP

This ledger is a living document that tracks the implementation progress, architectural decisions, and chronological build history of the Custom ERP system.

---

## 1. Project Genesis (Where We Started)
The project started from a static, client-side HTML dashboard template served via a basic HTTP server. All data operations (such as Brand and Style definitions) were mocked inside client-side variables (`db.js`) and simulated using `localStorage` data stores. There was no real database, no multi-tenant isolation, no numbering safety, and no observability.

---

## 2. Architectural Decisions & Rationale
We transitioned the mock frontend into a production-grade multi-tenant backend architecture:

| Component | Choice | Rationale |
| :--- | :--- | :--- |
| **Backend Runtime** | **Go (Golang 1.22)** | Single binary compilation, startup memory footprint < 15MB RAM per client, and extremely fast startup times (<10ms). Allows hosting high density client containers on cost-effective virtual servers. |
| **Database** | **PostgreSQL 16.3** | Supports **Schema-per-Tenant** isolation in a single instance. Provides transactional integrity (`SELECT ... FOR UPDATE` row locks) for inventory ledgers, sequence counters, and checks. |
| **Multi-Tenancy** | **Schema-per-Tenant** | Each tenant's data is isolated in a separate database schema (e.g. `tenant_default`). Resolves dynamically via a request middleware. Prevent data leakage and allows schema customization per client. |
| **Branding** | **Custom ERP** | Cleaned up all legacy sample branding (e.g. "Ditos") to establish a generic, copyright-safe, customizable system. |

---

## 3. Project Rollout Completion Ledger

```
[x] Phase 1: Core Foundation & Scale Infrastructure (Event Bus, Outbox, Availability, Reservations) -> COMPLETED
[x] Phase 2: Single Vertical Pilot (Jewellery PO-GRN-Barcode-Inventory-Transfers-POS-Finance) -> COMPLETED
[x] Phase 3: Omnichannel Pilot (Shopify Sync, Webhook Imports, Fulfillment Routing) -> COMPLETED
[x] Phase 4: Store Fulfillment (Ship-from-store, BOPIS, Return Anywhere, Tasks Dashboard) -> COMPLETED
[x] Phase 5: Scale Test (Simulate 100, 500, 1000, 2000 stores) -> COMPLETED
[x] Phase 6: Marketplace/OMS Expansion (Settlements, Logistics, Support Console) -> COMPLETED
[x] Phase 7: Advanced Optimization (Forecasting, Replenishment, SLA target tuning) -> COMPLETED
```

**Scope note (2026-07-12)**: Phases 1-7 above are the **Omnichannel Scale Add-on Blueprint's** rollout plan — they cover the kernel and omnichannel/scale backend, and "COMPLETED" here is accurate for that scope. It does **not** mean the full ERP described in the Master Blueprint is complete: POS/Finance/GST/CRM/HR/Assets UI, the approval/maker-checker workflow engine, MFA, and most of the ~80-report catalog were never part of this Phase 1-7 plan and remain unbuilt. See **[docs/pdf_blueprint_gap_analysis.md](specs/pdf_blueprint_gap_analysis.md)** for the full comparison against all 6 spec PDFs and `docs/micro_checklist.md` Stage 13 for the resulting backlog.

---

## 4. Phase 1 Build Records (What We Built)

### 4.1 Numbering/Sequence Engine
*   **Location**: `engines/numbering.go`
*   **Behavior**: Dynamically builds sequences: `<Prefix><Separator><StoreCode><Separator><FinancialYear><Separator><PaddedNumber>`.
*   **Concurrency Control**: Connects to DB, starts transaction, locks the current row inside `sequence_counters` via `SELECT ... FOR UPDATE` lock, increments, and commits. Prevents duplicate numbers under concurrent requests.

### 4.2 Dynamic Label Translation Engine
*   **Location**: `engines/labels.go` (Backend API), `app.js` (Frontend translation walker).
*   **Behavior**: Translates exact-match UI strings dynamically. The frontend traverses the DOM recursively using a TreeWalker node filter and updates labels on-the-fly based on translations cached from `/api/v1/labels`.

### 4.3 Audit & System Exception Hub
*   **Location**: `engines/logs.go` and panic recovery middleware in `main.go`.
*   **Behavior**: 
  - **Audits**: Writes structural log rows detailing user actions to `audit_logs` table.
  - **Panics**: Catches Go handler panics, retrieves stack trace, logs it to database under a unique correlation UUID, and returns HTTP 500. Rendered visually inside the Log Hub interface.

---

## 5. Phase 2 Build Records (What We Built)

### 5.1 DocType Metadata Builder & Registry APIs
*   **Location**: `engines/doctype.go` and routes registered in `main.go`.
*   **Behavior**: Registers custom document schemas dynamically (`POST /api/v1/meta/doctypes`) and registers custom column properties (fields name, labels, display orders, datatypes, and mandatory parameters) in `doctype_fields` (`POST /api/v1/meta/{doctype}/fields`).

### 5.2 Dynamic Form & Grid Rendering Interpreter
*   **Location**: `public/app.js` and `public/index.html`.
*   **Behavior**: Traverses dynamic field structures queried from `/api/v1/doc/{doctype}/meta` and constructs form fields (`Data`, `Number`, `Select`, `Link`) inside a single dynamic modal (`#dynamic-modal`). Automatically queries referencing list lookups for linked fields dynamically from the backend.

### 5.3 Parent-Child Vocabulary & Translation Engine
*   **Location**: `public/app.js` DOM translator.
*   **Behavior**: Dynamically maps vocabulary references per tenant, allowing users to override default nomenclature (such as mapping `Brand` to `Material Grade` or `Color` to `Fabric Metallurgy`) and translates UI elements instantly.

---

## 6. Phase 3 Build Records (What We Built)

### 6.1 Industry Profile Preset Configs & Loader
*   **Location**: `public/profiles/` directory and `SwitchIndustryProfile` in `engines/doctype.go`.
*   **Behavior**: Provides structural configuration mappings for Jewelry, Food & Beverage, Automobile, and Clothing industries. Loads templates dynamically via a database transaction: clears old field definitions, updates doctype metadata, registers customized columns, inserts default sequential counter templates, and resets translation vocabulary overlay caches.

### 6.2 Bulk Uploads Engine
*   **Location**: `engines/import.go` and HTTP route handlers in `main.go`.
*   **Behavior**: Parses raw CSV records, maps fields case-insensitively, validates formatting and constraints (numeric boundaries, date validity, select bounds, reference links), increments sequence codes dynamically inside a transactional context, and yields a structural validation report showing total successes and specific cell-level validation errors.

---

## 7. Phase 4 Build Records (What We Built)

### 7.1 Balanced Double-Entry Finance Engine
*   **Location**: `engines/finance.go` and `POST /api/v1/checkout`.
*   **Behavior**: Posts balanced journal entry lines (sum debits == sum credits) to `gl_postings`. Automatically handles bookings during checkouts: debits Cash (`1100`) & COGS (`5100`), credits Sales Revenue (`4100`) & Inventory Control (`1200`).

### 7.2 Store Fulfillment Task Manager
*   **Location**: `engines/fulfillment.go` and `POST /api/v1/fulfillment/task/transition`.
*   **Behavior**: Manages picking tasks (`FulfillmentTask`). If a task is rejected, automatically releases local reservations and triggers re-routing to the next best store node.

### 7.3 Return Anywhere Engine
*   **Location**: `engines/fulfillment.go` and `POST /api/v1/fulfillment/return`.
*   **Behavior**: Safely processes returns at any store, incrementing local stock levels and posting balanced refund GL journals.

---

## 8. Phase 5 Build Records (What We Built)

### 8.1 Concurrency Scale Simulation Engine
*   **Location**: `engines/scale.go` and `POST /api/v1/admin/scale-test`.
*   **Behavior**: Stress-tests 2,000 stores with parallel checkout workers, tracking throughput (TPS) and latencies. Verified ~456 TPS under 100 concurrent workers with 0 conflicts and a balanced post-simulation ledger.

---

## 9. Phase 6 Build Records (What We Built)

### 9.1 Marketplace Settlement & Reconciliation Engine
*   **Location**: `engines/marketplace.go` and `POST /api/v1/marketplace/settlement/reconcile`.
*   **Behavior**: Receives marketplace payout reports, validates payout math (`total - commission == net`), reconciles cart orders status to `Settled`, and posts balanced GL entries (debits Cash `1100` and Commission Expense `5200`, credits Accounts Receivable `1300`).

### 9.2 Carrier Logistics Dispatch Tracker
*   **Location**: `engines/marketplace.go` and `POST /api/v1/marketplace/logistics/book`.
*   **Behavior**: Registers carrier dispatch metadata (`LogisticsBooking` documents in `Shipped` status) to track carrier names, tracking numbers, and shipping charges.

---

## 10. Phase 7 Build Records (What We Built)

### 10.1 Sales-Velocity-Driven Replenishment Suggestions
*   **Location**: `engines/optimization.go` and `GET /api/v1/optimization/replenishment-suggestions`.
*   **Behavior**: Computes average daily sales velocity of SKUs based on POSCart checkout histories over 30 days and suggests replenishment orders (`suggestedQty = (velocity * leadTime) + safetyStock - available`).

### 10.2 Historical Sales-Based Forecasting Engine
*   **Location**: `engines/optimization.go` and `POST /api/v1/optimization/forecast`.
*   **Behavior**: Projects future SKU sales volumes based on daily historical velocity rates.

### 10.3 Picking SLA Breach Monitors
*   **Location**: `engines/optimization.go` and `GET /api/v1/optimization/sla-breaches`.
*   **Behavior**: Scans open fulfillment tasks, measures elapsed time since creation, and highlights tasks exceeding SLA thresholds.

### 10.4 Status-mismatch bug — fixed 2026-07-12
*   §10.1 and §10.2 used to compute sales velocity by querying `POSCart` documents with `status = 'completed'`, which the system never actually produces. Fixed to match `status IN ('Paid', 'Settled')` — the two real statuses `handleCheckout` and `ProcessMarketplaceSettlement` actually write. Verified with a real checkout followed by a real forecast call. See `docs/hardening_roadmap.md` Phase 2.2.

---

## 11a. SaaS Provisioning, Feature Flags & Integration Log Retries (Build Record)

*   **Tenant Provisioning**: `engines/saas.go`, `POST /api/v1/admin/tenant/provision`. Clones 19 table structures and seeds metadata from `tenant_default` into a new tenant schema. Fixed 2026-07-12 (`hardening_roadmap.md` Phase 1.6): `users` is no longer cloned into the seed loop — each new tenant gets one admin user with a unique, freshly-generated bcrypt-hashed password (`generateRandomPassword()`), not the shared placeholder hash.
*   **Feature Flags**: `engines/saas.go`, `POST /api/v1/admin/tenant/feature-flag`. Per-tenant boolean flags backed by a new `feature_flags` table. `SetFeatureFlag` is wired to the API; `IsFeatureEnabled` isn't called from any request handler yet, so flags don't currently gate any behavior.
*   **Integration Log Viewer & Retry**: `engines/outbox.go` (`GetIntegrationLogs`, `RetryIntegrationEvent`), `GET /api/v1/integration/logs` and `POST /api/v1/integration/retry`. Backend works; the frontend Log Hub view doesn't call either endpoint yet, so there's no integration-payload list or retry button in the UI.

For the full list of known gaps and the plan to close them, see **[docs/operations/hardening_roadmap.md](operations/hardening_roadmap.md)**.

---

## 11b. Real Login Flow (Build Record, 2026-07-12)

Closed `hardening_roadmap.md` Phase 1.1. Prior to this, `apiMiddleware` silently granted `HR/Admin` access to any request with no `Authorization` header — the app had no login screen at all and worked *only because of* that bug.

*   **Backend**: `main.go` `apiMiddleware` now rejects any request without a valid Bearer token, except `POST /api/v1/login` itself.
*   **Frontend**: new login screen (`public/index.html`/`app.js`/`styles.css`), logout button in the sidebar, and `apiFetch`'s 401 handling now logs the user out and returns to the login screen instead of a dead-end alert.
*   **Credentials**: all 4 seed users reset to unique bcrypt hashes (`db/migration.sql`) — the previous shared hash's plaintext was never recorded anywhere. Dev-only plaintext is in `DEV_CREDENTIALS.local.txt` (gitignored, project root).
*   **Token expiry (Phase 1.4)**: also closed 2026-07-12 — issued tokens now embed an `exp` claim (default 24h TTL, `JWT_EXPIRY_HOURS` overridable) and `ParseToken` rejects expired ones.

---

## 11. Version Control & Git History

*   **Remote Repository**: `https://github.com/Vishalnayak226/custom_erp.git`
*   **Branch**: `main`
*   **Latest Commit**: `ccbc539`
*   **Commit Message**: `Close Stage 13.13e: Manufacturing (scoped MVP - BOM + Production Order)`
*   **Date**: 2026-07-17

---

## 12. Stage 13 Build Records (What We Built, 2026-07-12 through 2026-07-17)

Closed the "business-user-facing ERP" gap identified in `docs/pdf_blueprint_gap_analysis.md` §1 — the codebase through Phase 7 was strong on kernel/omnichannel architecture but thin on the modules an actual end user touches day to day. Every item below shipped as its own commit, live-verified against a throwaway server instance before committing. Full scope decisions, bugs found/fixed, and verification detail live in `docs/micro_checklist.md`; this is a summary index, not the detail.

*   **13.1-13.3 Security & feature gating**: `securityHeaders` middleware (CSP/HSTS/X-Frame-Options/X-Content-Type-Options), `featureGate()` wrapper.
*   **13.4-13.7 Frontend for existing backend**: POS checkout screen, Finance/GL screen, Fulfillment screen, Marketplace screen (`public/app.js` `renderPOSView`/`renderFinanceView`/`renderFulfillmentView`/`renderMarketplaceView`). Fixed two location-filter bugs in `handleGenericDoc` where non-admin users saw zero results — first because `FulfillmentTask` stores `location_code` not `location`, then a deeper SQL `COALESCE(...) IS NULL` gap that hid records from doctypes with no location concept at all (e.g. `MarketplaceSettlement`).
*   **13.8 Maker-checker approval/workflow engine**: `engines/approval.go` — amount-slab + role + location routing, `approval_rules`/`approval_log` tables, `SubmitForApproval`/`DecideApproval`. Reused later by ExpenseClaim (13.13c) rather than rebuilt. A concurrent session later hardened `DecideApproval` against a TOCTOU double-decision race with a `FOR UPDATE` row lock (commit `bffea51`).
*   **13.9-13.10 Vendor/Customer masters + GST fields**: Vendor/Customer doctypes; Item `hsn_code`/`gst_rate` fields plus GST GL accounts.
*   **TOTP MFA**: `engines/mfa.go` — RFC 6238 from stdlib only (`crypto/hmac`, `crypto/sha1`, `encoding/base32`), enrollment/activation/challenge flow, required for HR/Admin roles only. Custom HMAC-signed purpose-tokens (`SignPurposeToken`) carry the MFA challenge state.
*   **13.11 Report catalog**: `engines/reports.go` — Current Stock, Sales Register, Vendor Ledger, Payables Ageing (4 prioritized reports, not the full ~80-report spec catalog).
*   **13.12 RFQ / Vendor Quote / Quote Comparison**: `engines/rfq.go` — `GetVendorQuotesForRFQ`, transactional `SelectWinningQuote` (winner→Selected, others→Rejected, RFQ→Closed).
*   **13.13a HR Foundation**: `engines/hr.go` — Employee/Attendance/Leave doctypes, Employee↔user access-link sync, payroll export.
*   **13.13b Fixed Asset Management**: `engines/assets.go` — capitalize/straight-line-depreciate/transfer/dispose lifecycle, asset register (fixed a display bug where disposed assets showed full original cost as net block instead of 0).
*   **13.13c Expense Management**: `engines/expense.go` — claim date-window + duplicate-bill validation, verify, pay (posts GL: Debit Employee Expense + GST Input Credit, Credit Cash/Bank). Reuses the 13.8 approval engine rather than a bespoke workflow.
*   **13.13d CRM/Loyalty (scoped MVP)**: `engines/loyalty.go` — append-only point ledger (earn/redeem, balance always `SUM(Earn)-SUM(Burn)`), wired into POS checkout as an additive side-effect. No campaigns/segmentation/vouchers.
*   **13.13e Manufacturing (scoped MVP)**: `engines/manufacturing.go` — single-level BOM explosion, linear Production Order (Draft → Material Issued → Completed), reuses the existing inventory stock-floor check. No routing/work centers, MRP, or QC gates.
*   **13.14 Per-API-type rate limiting**: `RateLimiter` keyed `ip:category` (login/bulk-upload/report/webhook/search/default) instead of just `ip` — fixed a real bug class where heavy traffic on one endpoint exhausted the shared budget for an unrelated one.
*   **13.15 Sticker/barcode printing**: `engines/stickers.go` — print + history, `@media print` CSS label layout. Scoped to text labels, not real scannable symbology (unverifiable in this dev environment).

For the current build tracker (what's still open), see **[docs/micro_checklist.md](micro_checklist.md)**.

---

## 13. Stage 14-17 Build Records (What We Built, 2026-07-18 through 2026-07-19)

Full scope decisions, bugs found/fixed, and live-verification detail for every item below live in `docs/micro_checklist.md` (Stages 14-17); this is a summary index, not the detail, matching the convention §12 established for Stage 13.

*   **Stage 14 — Control plane, versioning, module governance, patch automation, extension isolation, security hardening.** A real multi-environment deployment pipeline (`environments.json` + `promote.ps1` + `manage.ps1 -Env` — git-worktree checkout → stripped native binary → restart, one Postgres instance per environment on its own port); module/feature governance (`public.modules`/`tenant_default.module_entitlements`, `moduleGate()`); a patch/bug-intake pipeline with worker wiring; 3rd-party extension isolation and source-protection controls; security hardening (account-level brute-force lockout with a real timezone-comparison bug found and fixed during live verification, Shopify inbound webhook HMAC signature verification, a generic `VerifyWebhookHMAC` helper for future webhooks); Go toolchain bumped 1.22.5 → 1.22.12 (30 → 27 reachable `govulncheck` CVEs — the rest need a 1.23+ major-version jump, not attempted). **Containerization was built on explicit request (14.25), then reverted on explicit request (2026-07-19)** — the user's standing preference is no Docker dependency in this project's workflow; `promote.ps1`/`manage.ps1 -Env` remains the one real deployment path.
*   **Stage 15 — PIM (Product Information Management) Foundation MVP + V2 alignment.** `engines/pim.go`: Family/Attribute framework as generic doctypes, parent/variant grouping on `Item`, approval-gated `ProductContent` (reuses the Stage 13.8 approval engine, zero new approval code), a completeness-scoring engine, and a Product Workbench screen. V2 added locale/channel-aware completeness, an 8-state `PIMProductProfile` publish lifecycle, a genuinely new Media Library (`engines/pim_media.go` — this codebase's first file-upload infrastructure: content-addressed storage, MIME allowlist + content-sniffing, auth-gated retrieval outside `public/`), a Channel Publishing framework (`engines/pim_publish.go` — queue/idempotency/retry machinery real, connector itself a stub pending real platform credentials at the time), and job-tracked CSV Import/Export preview. AI Assist stayed explicitly out of scope.
*   **Stage 16 — Real e-commerce channel connectors + remaining PIM gaps.** A connector framework (`engines/connector.go`) with an AES-256-GCM encrypted credential vault (`engines/channel_credentials.go`) and real, code-complete connectors for Shopify, BigCommerce, and Magento/Adobe Commerce (`engines/connector_shopify.go`/`connector_bigcommerce.go`/`connector_magento.go` — each unit-tested against an `httptest` fake platform; live-store verification explicitly deferred until real dev-store credentials are supplied). Closed the remaining PIM gaps: dashboard (`engines/pim_reports.go` `GetPIMDashboard`), bulk edit (`engines/pim_bulk.go`), the 4 PIM reports, and field-level permissions (`engines/field_permissions.go` + `db/migrations_stage16_field_permissions.sql`). The same commit (`c09d72e`) also landed Stage 9 integration connectors (`engines/clevertap.go`, `engines/pinelabs.go`, `engines/unicommerce.go`) and Log Hub frontend wiring — recorded here for the index; see `docs/micro_checklist.md` and the commit itself for their scope detail, not independently re-verified in this summary pass.
*   **Stage 17 — Controlled post-PIM execution queue.** **17.1** generic-document soft delete (additive `deleted_at` tombstone, `SOFT_DELETE_<doctype>` audit event, master reactivation endpoint, Approved transactional documents blocked from deletion). **17.2** CSV formula-injection protection (`sanitizeCSVCell`, applied to every import and export/error-file path). **17.3** backup/restore baseline (`manage.ps1 backup`/`restore -Env`, SHA-256 sidecars, a real restore drill recorded in `docs/backup_restore.md`). **17.4** accounting-period control (`engines/accounting_periods.go` — admin-managed periods, `PostDoubleEntry` rejects postings inside a Closed one). **17.5** GST posting enforcement at both PurchaseOrder creation and checkout (`engines/gst.go` — HSN/rate gate, tax-inclusive-rate breakdown auto-computed and stored, checkout posts the split to new `2200`/`2201`/`2202` GL accounts). **17.6** transfer-order dispatch/receive lifecycle (`engines/transfer_orders.go` — new `in_transit` column, row-locked, shortage variance recorded not silently reconciled). **17.7** purchase requisition workflow (`engines/procurement.go` — reuses the existing approval engine unchanged; one-time Approved→Draft-RFQ/PO conversion). **17.8** vendor invoice + three-way match + payment (`engines/vendor_invoice.go` — DB-level duplicate-invoice constraint, `Match3Way` genuinely uses GRN's received quantities against the PO's own rates, payment posts GL before flipping status). **17.9** Location/LegalEntity/Department/CostCenter masters (`engines/location_masters.go` — 103 legacy location codes seeded so validation never breaks existing data; wired into PurchaseOrder/TransferOrder only, explicitly not a blanket retrofit). **17.10-17.11**: code/docs/tooling built and verified per §14 below; each still has one real-world input only the user can supply before it's fully DONE. See `docs/micro_checklist.md` Stage 17 for full detail.

---

## 14. Stage 17.10-17.11 Build Record (What We Built, 2026-07-19)

Both items were blocked on real-world input the AI cannot supply (operational contacts; non-production platform credentials) rather than on implementation — per user direction, everything buildable ahead of that input was built and verified this session. Full detail in `docs/micro_checklist.md`'s 17.10/17.11 entries.

*   **17.10 Runbook and alerting.** `docs/operations/incident_runbook.md` (P0-P3 severity table, rollback via `promote.ps1 -Rollback`/`manage.ps1 restore`, log locations, escalation-contacts table left as an explicit placeholder). `engines/alerting.go`: `SendOpsAlert` posts a truncated, secret-free summary to a Slack/Teams-compatible webhook (`OPS_ALERT_WEBHOOK_URL`, safe no-op when unset); `StartAlertMonitor` polls for a sustained error rate (20+ `system_error_logs` rows/5 min per tenant, one alert per cooldown window). Panic recovery (`engines/logs.go`) and failed backups (`manage.ps1`'s new `Send-OpsAlert`) both wired to the same mechanism. 4 new unit tests (`engines/alerting_test.go`); `go build`/`vet`/`test ./... -p 1` clean. **Live-verified** on a throwaway instance against a local mock webhook receiver (no real Slack/Teams destination existed to point at): triggered the existing `/api/v1/debug/panic` route, confirmed both the `system_error_logs` row and the webhook payload (severity/source/message only, no stack trace) arrived correctly. Open: real escalation contacts and a real `OPS_ALERT_WEBHOOK_URL` — the code path is proven, only the destination is missing.
*   **17.11 Live connector verification.** `scripts/verify_connector_live.ps1`: given real credentials (`CONNECTOR_CREDENTIALS.local.json`, gitignored) and a pre-approved disposable Item, creates a throwaway Channel, runs the existing PIM readiness check, triggers a real publish, polls to a terminal status, reports the platform's own external id or rejection, then cleans up its own fixtures. Deliberately does not script Item/ProductContent creation itself (that stays on the already-verified PIM Workbench + approval UI path) or login/MFA (no TOTP secret stored on disk for a one-time script). `docs/operations/connector_live_verification.md` documents the exact credential shape (read directly from each connector's source, not guessed) and procedure. Both PowerShell scripts verified to parse with zero syntax errors. Not run end-to-end — no real store credentials exist in this environment, which is this item's entire remaining blocker.

---

## 15. Stage 19 Build Record — Documentation Suite & Folder Restructuring (What We Built, 2026-07-19)

Per explicit user request, in stated order: a full project blueprint for outside/AI review, a real folder restructuring ("as per standard development practice" — the user chose the full `cmd/`+`internal/` Go split over a lighter-touch option when asked, accepting the added risk explicitly), a `docs/` reorganization with nothing deleted, and a new BRD/PRD/User Guide/Admin Guide. Full detail and verification steps: `docs/micro_checklist.md` Stage 19.

*   **`docs/ERP_BLUEPRINT.md`** — a full project snapshot for an external reader, five-persona framing (CEO/Product/CTO/Developer/Tech-lead), every claim cited back to an existing source doc.
*   **Go restructuring.** `main.go` (~4,681 lines, package `main`, root of the repo since Phase 1) moved to `internal/server/` (renamed `package server`), split into 8 domain files within that same package, with a new thin `cmd/server/main.go` entrypoint. The riskier alternative — decomposing into several separate *packages* — was deliberately scoped out as separate future work; this stayed a same-package reorganization, so no cross-package visibility risk. Every build command that assumed repo-root-as-package-main (`manage.ps1`, `promote.ps1`, including an `-ldflags -X main.gitCommit=...` target that had to become a full import-path reference) was updated and re-verified. `go build`/`vet`/`test ./... -p 1` clean throughout; live-verified via a fresh binary build and smoke test (login, version, RBAC-denied doc read, static file serving, panic recovery) on a throwaway port. Found and fixed, while in the area: `promote.ps1`'s `$args` automatic-variable shadowing and 3 PSScriptAnalyzer unapproved-verb function names.
*   **`docs/` reorganization.** New `architecture/`, `specs/`, `operations/`, `requirements/`, `guides/` subfolders; `micro_checklist.md`/`project_ledger.md`/`ai_handover.md`/`ERP_BLUEPRINT.md` stay at `docs/` root per `CLAUDE.md`'s hardcoded sync-convention paths. New `docs/README.md` index. Nothing deleted — every cross-reference (clickable links and prose path citations) fixed to the new locations across the whole repo, verified by a full grep sweep; historical narrative mentions left untouched.
*   **BRD/PRD/User Guide/Admin Guide.** Grounded in three external planning references the user pointed to mid-session — extracted from `.docx` to plain text since they're binary — which describe a considerably larger target platform (full warehouse bin/putaway/pick-pack, offline POS, e-invoicing/IRN, an ~80-report catalog, a formal error-proofing matrix) than what's built today. Every status claim in the new documents is checked against `micro_checklist.md`, not assumed from those references. User Guide is plain-language/non-technical; Admin Guide is a standalone, zero-AI-assistance operator manual layered from first-time setup through developer/CTO-level detail, reusing rather than duplicating the existing operations docs.

---

## 16. Stage 18 Build Record — Data-Entry UX: Dropdowns/Autosuggest (What We Built, 2026-07-19)

Per explicit user request ("either all value should be in dropdown or auto suggest... without this, this will be the most difficult platform to handle"). Full field-by-field audit, decision rationale, and live-verification detail: `docs/micro_checklist.md` Stage 18.

*   **Audit first.** Checked every hand-built create/edit form in `public/app.js` plus the generic DocType Builder engine's field metadata against the real doctype/master landscape. Found the two examples originally named for this stage don't exist as buildable targets: no `ChartOfAccounts` doctype (GL accounts are a raw SQL table with no CRUD screen) and no `GRN`/`VendorInvoice` doctype/screen - both flagged as gaps rather than silently dropped. Scoped the actual work to the real free-text master-reference fields found instead (Vendor/Location/Customer/Item/Employee across PurchaseOrder, POS, RFQ, HR Attendance, Assets, Expenses, Manufacturing).
*   **Stayed UI-only by design.** Mirrors the call Stage 17.9 already made (`db/migrations_stage17h_location_masters.sql`) against retrofitting hard `Link`-field validation onto existing free-text columns. No `doctype_fields.fieldtype` changed, no new server-side validation added - a typed value that matches nothing still saves exactly as before; the picker only suggests.
*   **`attachTypeahead(inputEl, doctype, opts)`** — one reusable component in `public/app.js`, backed by the existing generic `GET /api/v1/doc/{doctype}?q=...` search: debounced, keyboard nav, click-outside-to-close, stale-response guarded. Wired into 12 fields across 7 screens (PurchaseOrder vendor/warehouse/location, POS location/customer/SKU, RFQ vendor, HR Attendance location, Assets location/custodian, Expenses location, Manufacturing item/location), plus fixed Marketplace's hardcoded Shopify/Amazon-only channel `<select>` to also pull real `Channel` records (additively, so it can't regress to an empty dropdown).
*   **Two real bugs found and fixed during live verification** (Playwright against the actual running dev server + DB, not just typechecked):
    1. **In this stage's own new code**: `openMenu()` called `closeMenu()` to clear a stale suggestion box, but `closeMenu()` also wiped the just-fetched `items` array as a side effect - so the menu could never render, for any query, on any field, regardless of how many real matches came back. Fixed by separating "remove the stale DOM element" from "fully dismiss and clear state."
    2. **Pre-existing, in Stage 17.9's own migration**: `Location`/`LegalEntity`/`Department`/`CostCenter` were registered under `module_key = 'core'`, but `'core'` was never added to the `public.modules` catalog or granted in any tenant's `module_entitlements` - so every read/write to these four doctypes through the generic doc endpoint has been returning `403 "Module 'core' is disabled for this tenant"` for every role, on every tenant, since Stage 17.9 shipped (undetected because `ValidateLocationReference`, the only path exercised at the time, bypasses this module gate). Fixed via `db/migrations_stage18_core_module_fix.sql`, applied live to `custom_erp`: registers `'core'` in the catalog and backfills an enabled entitlement into both existing tenant schemas; new tenants inherit it automatically via the existing provisioning-copy path.
*   **Found, not fixed - flagged for the user**: `role_permissions` currently gives read access to `Vendor`/`Item`/`Customer` to `HR/Admin` only. `Store Manager` and `Cashier` - the roles that actually use Purchase Orders and POS day to day - get `403` when the new typeaheads try to fetch suggestions on those masters, so the pickers are code-correct but not yet practically usable by their intended users. Left as a `[needs product decision]` in the checklist rather than silently expanding role permissions, since that's a different risk category (RBAC/security policy) than this stage's UI-only scope.
*   **Verified**: `node --check public/app.js` clean throughout. Live-verified end to end via Playwright against the real dev server/DB - typed queries, confirmed suggestions render and are pickable (after both bug fixes above), confirmed non-matching free text is retained with no blocking/error, confirmed existing button-click flows (PO create, POS add-to-cart) still reach the server unchanged, zero JS exceptions across the run. No Go files touched this stage (frontend/SQL only), so `go build`/`vet`/`test` were not re-run.

---

## 17. Stage 21 Build Record — UI/UX Overhaul: Scroll Fix, Native-Popup Cleanup, Account Menu, User Profile (What We Built, 2026-07-20)

Started from two concrete bugs the user screenshotted (Finance/GL table clipping with no scroll; DocType Builder's "Enter DocType Name" still a raw OS prompt), then broadened per the user's own choices into an icon-minimal (SAP/MS-style) visual pass, an account-menu/sign-out redesign, and a new self-service User Profile screen. Ran concurrently with another session's work landing Stage 18's commit and drafting the Stage 20 maturity-plan backlog in the same files — this stage's own migration/Stage-comment numbering was caught colliding with that Stage 20 and renumbered to 21 before anything shipped. Full detail: `docs/micro_checklist.md` Stage 21.

*   **Root cause, one shared CSS rule.** `.table-panel` (styles.css) — the card wrapper used by roughly 50 render functions app-wide for every list/table and most forms — was `overflow: hidden` inside a flex column layout that already sized it correctly to available space; it silently clipped instead of scrolling. One-line fix to `overflow: auto` made the pre-existing `th { position: sticky; top: 0 }` rule work everywhere with no per-page changes. Same audit turned up and fixed two more live CSS bugs: a `.stat-value` CSS rule that never matched the `stat-val` class app.js actually renders (KPI numbers were unstyled), and dead `.stat-icon`/`.card-icon` rules.
*   **Killed the last 4 raw `window.prompt()` calls** (loyalty redemption, asset transfer ×2, asset disposal) — migrated to the `showCustomPrompt` dialog the rest of the app already used.
*   **Icon-minimal sidebar**, the user's explicit choice over refining the existing icon set: removed all 26 per-menu-item decorative SVGs from `index.html` (3 were exact-duplicate path data on unrelated concepts), kept icons only as universal utility affordances (search, sync, dialog close, account-menu chevron).
*   **Account menu + sign-out redesign.** The old static sidebar-footer (avatar/name/role + a bare logout icon) became a single clickable trigger opening a popover (name/email/role, My Profile, Sign Out); sign-out now shows a brief transition overlay before the login screen instead of an instant cut.
*   **New User Profile screen**, reachable from the account-menu popover: `GET/PUT /api/v1/me` + `POST /api/v1/me/change-password` (`internal/server/handlers_profile.go`, same header/bcrypt patterns as every other auth-adjacent handler), a new additive `users.idle_timeout_minutes` column, and a best-effort `Employee.user_id → user` lookup (previously write-only in the other direction). Frontend `renderProfileView` with Account Info / Contact & Session / Change Password cards. The saved idle-timeout preference drives a new client-side inactivity timer, separate from and shorter than the server-side JWT session TTL.
*   **A bug found live, not in the original ask**: an early version of the sidebar-highlight-on-refresh fix used `scrollIntoView({block:'nearest'})` on every click, which snapped the active item flush to the sidebar's bottom edge on ordinary navigation, not just on refresh. Fixed by only scrolling once, on page-load restore, and centering instead of edge-snapping.
*   **Verified**: `go build`/`go vet`/`go test ./... -p 1` clean; migration applied live to `custom_erp`; a Playwright pass against a throwaway port (8099, the user's own dev session on 8080 left untouched) covering login through Finance/GL scroll, sidebar icon count, the account popover, all three Profile cards, and the refresh/scroll-position fix, zero console errors beyond the browser's own harmless `favicon.ico` 404. Added a new "lightweight, future-proof, solid" first-principle section to `CLAUDE.md` per the user's own mid-session request, applied throughout this stage (no new frontend framework, no new dependencies, additive-only migration, reused existing dialog/CSS patterns rather than a third parallel one).

---

## 18. Stage 20 Track B.1 Build Record — POS Maturity (What We Built, 2026-07-20)

First slice of the Stage 20 ERP-maturity backlog (`docs/specs/erp_maturity_master_plan.md`), built after the user asked to start on Stage 20's open items and picked POS Maturity as the first of its four buildable tracks. Full item-by-item detail: `docs/micro_checklist.md` Stage 20 Track B.1 (20.6-20.15). Ran in the same shared tree as another session's Stage 21 work (`handlers_profile.go`, the User Profile screen) — left those files' content untouched throughout, only adding to `app.js`/`index.html`/`styles.css` alongside them.

*   **20.6 POS Profile master** — pure `doctype_meta`/`doctype_fields`/`role_permissions` registration (`db/migrations_stage20a_pos_maturity.sql`), same generic Master-doctype pattern Location/Vendor already use; a "POS Profiles" nav entry points at the existing generic doctype-table view. No new backend code needed for this one.
*   **20.7/20.8 Cashier sessions + cash variance** — new `POSSession` doctype, deliberately granted **no** generic create/update permission to any role (unlike `POSCart`, which technically still can be written directly via the generic doc API - a pre-existing gap left alone as out of scope) — every write goes through new `engines/pos_session.go` + `POST /api/v1/pos/session/{open,close}` / `GET /api/v1/pos/session/current`, with cashier identity always taken from `Resolved-Username`, never the request body. `handleCheckout` now requires an Open session for the acting cashier/location. Closing computes `expected_cash` server-side from the opening float plus every Cash-mode Paid `POSCart` tagged with that session (a new `pos_session` key `handleCheckout` stamps onto the cart at checkout time) — never trusts a client-supplied expectation.
*   **20.9 Payment-mode-aware GL posting** — `payment_mode` was already captured and reported on since Stage 13/17, but `PostSalesFinanceBooking` posted every sale to GL account 1100 regardless. It now takes the payment mode and posts to 1100 (Cash), or two new accounts, 1101 (Card Clearing) / 1102 (UPI Clearing). Split-tender (multiple modes on one cart) explicitly deferred - the POS screen only ever collects one mode per cart today; splitting that is a bigger data-model change than this item scoped.
*   **20.10 Discount-approval gate** — routes a large-discount POS sale through the *existing* `engines/approval.go` maker-checker engine exactly as the checklist item specified, not a new mechanism: a gated checkout claims its cart as `Draft` and calls the existing `SubmitForApproval` instead of running side effects; a manager decides via the existing `POST /api/v1/approval/decide`, which now finalizes the sale (inventory/GL/loyalty) only on `Approved`, via a new shared `engines.FinalizePOSCheckout` extracted from `handleCheckout`'s old inline logic so the synchronous and post-approval paths can never compute different totals. `extractAmount`'s key list grew a `discount_amount` entry (holds a percentage for `POSCart`, not rupees, so it can't collide with other doctypes' rupee-`amount` routing).
*   **A real bug found and fixed while building 20.10, not the original ask**: `handleCheckout`'s idempotency-claim INSERT hardcoded `created_by = 'system'` for every POS sale ever made. `DecideApproval`'s maker-checker check (`actor == created_by` → reject) compares against that column, so a Store Manager approving their *own* discount-gated sale would have silently passed the check that's supposed to block exactly that. Now uses the real acting user's ID. Confirmed via `grep` that nothing else in the codebase depended on `POSCart.created_by` being the literal string `'system'`.
*   **20.11/20.12/20.15 audited first, per each item's own instruction.** 20.12 (manual bill entry) and 20.15 (Pine Labs terminal reconciliation) were already fully satisfied by Stage 13.4/18 and Stage 9.1 respectively - confirmed via direct code read, nothing changed. 20.11 (returns) was a real gap: `engines.ProcessReturnAnywhere` / `POST /api/v1/fulfillment/return` (Stage 7.2/13.6) fully works but had zero frontend callers anywhere in `app.js` - added a "Process a Return" panel to the POS screen with its own cart state, kept deliberately separate from the sale cart so an in-progress return can never merge into an in-progress sale.
*   **20.14 Receipt printing** — reused Stage 13.15's hidden-until-`.printing` `@media print` pattern verbatim (second `#receipt-print-area` div, `styles.css`'s print rule extended to allow either target), offered via a confirm dialog after a completed sale. No new PDF/print dependency.
*   **20.13 (offline-first queue) deliberately not started** — flagged in its own checklist entry as needing a product decision (acceptable offline window; conflict behavior if stock changed while offline) before any code gets written.
*   **Verified**: `go build`/`go vet`/`gofmt -l`/`go test ./... -p 1` all clean. Migration applied live to the shared `custom_erp` dev DB (additive-only). Full flow live-verified end-to-end against a **throwaway server instance on a separate port** (the shared dev session on 8080 left completely untouched and still running throughout) with disposable Cashier/Store Manager test users: session-required 400 → session opened → duplicate-session-open rejected → Card sale posted to GL 1101 not 1100 → 15%-discount sale returned `pending_approval` → the submitting cashier's own approval attempt correctly rejected as a maker-checker violation → a different Store Manager's approval finalized the sale (inventory decremented, GL posted, status `Paid`) → session close computed `expected_cash`/`variance` exactly right against hand-verified arithmetic → the new POS-side return panel's backend call returned `refunded` → a direct generic-API write attempt against `POSSession` correctly 403'd. All test data cleaned up from the shared DB afterward.

---

## 19. Stage 22 Build Record — Full Page-by-Page QA Sweep + Guide Docs Walkthrough (What We Built, 2026-07-20)

Per explicit user request: test every page, judge whether each genuinely works (not just "doesn't error"), literally walk through the guide docs' own steps for a real task rather than trust them, fix what's broken, and correct the docs — codified as `CLAUDE.md`'s new "Second principle." Full item-by-item detail: `docs/micro_checklist.md` Stage 22.

*   **The sweep.** Built an isolated, additive-only HR/Admin test identity (fresh user inserted directly, MFA-enrolled through the real API flow rather than reading any secret out of the DB) so admin-only screens could be tested without touching the real `admin` account. Playwright hit all 60 reachable pages against a throwaway port - zero console errors, zero 5xx, zero missing titles. The one real finding: **Inventory, Transfers, Users, and Roles were all dead "Module Setup Pending" mocks** - their click handlers called the router with a view name it had no case for. Pre-existing, not a regression from this session's own earlier UI work.
*   **Fixed all four**, each reusing existing infrastructure rather than inventing a parallel one: Inventory wraps the already-live `/api/v1/reports/current-stock` endpoint with a real search box; Transfers is a bespoke form+list+action-button view (mirroring `renderAssetsView`'s existing shape) driving `engines/transfer_orders.go`'s already-built dispatch/receive lifecycle, which had no line-item input anywhere until now; Users and Roles are new HR/Admin-only endpoints (`internal/server/handlers_admin_identity.go`) since `tenant_default.users`/`role_permissions` are raw SQL tables the generic doctype engine can never reach. Roles is deliberately read+write, not read-only - it's now the tool to close the exact "Store Manager/Cashier can't read Vendor/Item" gap Stage 18 flagged and left open, though making that specific call is still left to the user, not decided here.
*   **Two real bugs found live, not in the original ask**: the generic doc engine's update replaces the whole JSONB blob rather than merging (would have silently wiped a transfer's other fields on a naive status-only edit - worked around, not changed, since fixing the engine itself is a much bigger blast radius than this pass scoped); a raw Postgres constraint-violation string was leaking to the UI on a duplicate username.
*   **Docs**: `USER_GUIDE.md` gained the master-data and transfers walkthroughs that never existed, a new Profile/session section, and an honest correction to its "role-filtered sidebar" claim (every role currently sees every menu item; only the actions are actually gated). `ADMIN_GUIDE.md` §B.2 replaced a vague pointer with the concrete steps the now-real Users/Roles screens support.
*   **Verified**: `go build`/`go vet`/`go test ./... -p 1` clean; every fix live-verified via Playwright including the full Transfers Draft→Approved→Dispatched→Received lifecycle against real seeded stock. All QA test data removed afterward (deleting the test admin user cascaded its own test documents via the existing `documents.created_by ... ON DELETE CASCADE` FK; the one live permission grant made during Roles-screen testing was explicitly reverted, not left in place, since granting it for real is a product decision that's still open).
*   **Flagged, not fixed**: the sidebar itself isn't role-filtered at all (cosmetic-only currently — every role sees every item, RBAC is 100% server-side) — a real but separate, cross-cutting feature, tracked as Stage 22.6 rather than folded into this pass.

---

## 20. Stage 20 Track B.2 Build Record — WMS Maturity (What We Built, 2026-07-20)

Second Stage 20 track, built immediately after Track B.1 (POS Maturity, §18) in the same session. Full item-by-item detail: `docs/micro_checklist.md` Stage 20 Track B.2 (20.16-20.24).

*   **20.16 Bin master** — pure metadata registration (`db/migrations_stage20b_wms_maturity.sql`), same generic Master-doctype pattern as Stage 20's own POSProfile.
*   **20.17 Putaway + 20.23 Damaged/QC/RTV** — new `tenant_default.bin_stock` table: a bin+condition breakdown of the *same* on-hand quantity `inventory_availability` already tracks at the location level, never a second source of truth. `engines.PutawayToBin` refuses to assign more than a location's unassigned on-hand; `engines.TransitionBinStockCondition` moves stock between Good/Damaged/QC-Hold/RTV, syncing `inventory_availability.available` (not `on_hand`) the same way the existing reserved-stock pattern already does.
*   **20.18 Pick list generation** — a computed (not stored) bin-grouped, zone/aisle/rack-sorted pick list for a `FulfillmentTask`'s items, greedily allocated across Good-condition bins with a per-SKU shortfall reported rather than a hard error.
*   **20.19 Pack/dispatch box mapping** — extends `TransferOrder`'s existing Stage 17.6 lifecycle with an optional `Approved` → `Packed` step (box/carton-to-item mapping); `DispatchTransferOrder` now accepts either `Approved` or `Packed`, so the already-shipped direct-dispatch flow keeps working unchanged. New "Pack" button on the Transfers screen.
*   **20.20/20.21/20.22 Cycle count, physical upload, and variance-approval** — `CycleCountLine` is one document per counted SKU (not a nested-array parent) specifically so the existing Stage 3.3 `BulkImportCSV` engine can import counted quantities with zero new import code. Reconciling fills in `system_qty`/`variance` from real `inventory_availability`; zero variance posts immediately, non-zero variance reuses `engines/approval.go` exactly as Stage 20.10's POS discount gate did, and `handleDecideApproval` finalizes the actual inventory adjustment only on `Approved`.
*   **A real, serious bug found and fixed during live verification, not the original ask**: the shared `numFromInterface` quantity-parsing helper only handled JSON `float64`/`int`, silently returning `0` for `string` values — the type every field `BulkImportCSV` populates actually is (raw CSV cells are stored as strings, never coerced to the doctype's declared field type). Every CSV-imported `counted_qty` was therefore read as zero, computing a wildly wrong variance: a live test run posted a **-100** inventory adjustment where the correct answer was **-10**, zeroing out real on-hand stock. Fixed by teaching the helper to parse numeric strings via `strconv.ParseFloat`, then re-verified the entire reconcile→approve→post pipeline end-to-end across three scenarios (zero variance, a shortfall, and an overage) with an *independent* second approver, confirming both the fix and that maker-checker correctly blocks the original CSV-importer and the submitting user from self-approving.
*   **20.24 audited, confirmed already satisfied** — `sticker_print_log` (Stage 13.15) plus the inventory ledger already cover print history and stock movement separately; no code changed.
*   **Verified**: `go build`/`go vet`/`gofmt -l`/`go test ./... -p 1` all clean. Migration applied live to the shared `custom_erp` dev DB. Full flow live-verified end-to-end against a throwaway server instance with disposable test users (including a second, independent approver specifically to exercise the maker-checker happy path, not just the rejection cases): putaway over-allocation correctly rejected; pick-list generation correct; transfer pack→dispatch correct; bin condition transitions correctly synced `available` while leaving `on_hand` untouched; and, after the fix above, all three cycle-count variance scenarios posted the mathematically exact correct adjustment. All test data cleaned up from the shared DB afterward.

---

## 21. Sidebar Navigation Redesign + Terminology Renames (What We Built, 2026-07-20)

Per explicit user request, screenshotting the live app mid-session: internal Frappe/ERPNext-style names ("DocType Builder") meant nothing to a general audience, and the 27-item flat sidebar had grown unwieldy.

*   **Renames**: "DocType Builder" → "Database Schema Design" (user's own suggested name); "Log Hub" → "Activity Log". Applied everywhere user-facing (sidebar labels, page titles, dashboard quick-launch cards) and in current-state docs (`ADMIN_GUIDE.md`, `UAT_CHECKLIST.md`, `README.md`'s file-structure comment, relevant code comments). Historical build-record entries in this file / `micro_checklist.md` / `ai_handover.md` deliberately left unchanged, per this repo's existing convention of not rewriting history to match a later rename. A full repo-wide grep swept every remaining mention to confirm nothing user-facing was missed.
*   **Sidebar regrouped by module**: ~27 individual top-level screen links collapsed into 12 module-level entries (POS, Finance, Fulfillment & Marketplace, Reports, Procurement, Inventory & WMS, HR & Assets, Manufacturing, PIM, Master Definition, Admin & Settings, plus standalone Dashboard), each revealing its actual screens in a hover flyout - matching a reference screenshot the user provided of a browser bookmarks-bar-style hover menu. Every existing screen's `id`, click handler, and `renderView()` call was left completely untouched - only the surrounding HTML structure (grouped into `<li class="has-flyout">` wrappers) and the show/hide mechanism changed, so no per-screen JS needed rewriting. Flyout position is computed in JS (`getBoundingClientRect()`), not CSS `position:absolute`, specifically so it's never clipped by the sidebar's own `overflow-y:auto` scroll container. The pre-existing Master Definition dropdown (dynamically populated with ~25 registered master doctypes) was unified into the same mechanism for visual consistency, replacing its old bespoke click-to-expand toggle.
*   **A real bug found and fixed during live (Playwright) verification**: the flyout's `max-height` was a flat `calc(100vh - 24px)`, which ignored *where* the trigger sat vertically - Master Definition's 25-item flyout, anchored near the bottom of a 12-item sidebar, ran off the bottom of the viewport. Fixed by computing `max-height` per-instance from the actual space below the trigger (`window.innerHeight - rect.top - margin`) rather than a flat viewport-relative value; re-verified the fix brought the flyout's bounding box fully within the viewport.
*   **Verified**: `node --check public/app.js` clean. Live-verified via a Playwright script against a throwaway instance (Chromium installed for this purpose - a local dev-only tool, not a project dependency, never shipped) covering: 12-entry sidebar count, flyout open-on-hover with correct unclipped positioning for both a short (POS) and long (Master Definition, Inventory & WMS) flyout, click-through navigation with active-state propagating to the parent module trigger, flyout auto-close on mouse-away, and highlight state correctly restored after a full page refresh. Two incidental `403` console errors during the run were confirmed pre-existing/expected RBAC (the test used a Store Manager account against masters that role has no read access to - the same Stage 18-documented gap, unrelated to this change).
*   **Also rebuilt and restarted the user's own long-running dev server (port 8080)** with a freshly built binary once all of the above (plus the same session's Stage 20 Track B.1/B.2 and the concurrent session's Stage 21/22 work) was ready, so the live session the user was actively looking at picked up every new route/screen instead of 404ing or showing stale UI text.
*   **Follow-up fix, then reverted (same day, live user report with screenshot)**: tried dimming the content pane behind an open flyout (a `.flyout-backdrop` element reusing the `.modal-overlay` fade pattern) so it wouldn't visually collide with the active screen underneath. Live user feedback after seeing it: the fade made the whole page feel like it was constantly transitioning ("sunrise/sunset") every time a flyout opened/closed while scanning the sidebar - undesired. Reverted in full (`public/index.html`/`styles.css`/`app.js`); the flyout's existing `z-index:100` opaque card is enough to render on top, no backdrop needed.
*   **Database Schema Design (`renderDocTypeBuilderView`) doctype list grouped by module, then converted to a hover-flyout (2026-07-20, live user request, two iterations)**: first pass grouped the flat doctype list under module headings (`.doctype-list-group-label`). User's live follow-up: wanted it to match the sidebar exactly - "Only module will be there. I will hover and select" - so superseded that with the real thing: the left panel now lists only module names, each a `.has-flyout` trigger whose flyout lists that module's doctypes, reusing the sidebar's own `setupModuleFlyouts()`/`openFlyout()`/`closeSubmenus()` (no parallel mechanism). Since that function previously ran once at `init()` over the sidebar's own static markup, made it idempotent (`container.dataset.flyoutBound` guard, plus a `moduleFlyoutDocListenersBound` flag around the once-only document/window listeners) so `renderDocTypeBuilderView` can safely call it again for its own dynamically-injected `.has-flyout` elements without double-binding the sidebar's. The now-unused `.doctype-list-group-label` CSS from the first pass was removed. Confirmed live against `tenant_default.doctype_meta` that every doctype already carries a non-empty `module` value (12 distinct modules), so this is pure UI, no new data.
*   **Remaining "DocType" UI text renamed to "Record Type" (2026-07-20, live user follow-up - the earlier rename commit `e2549ad` only changed the screen title, not every string on/around it)**: swept every *currently-reachable* user-facing string still saying "DocType" (the "Register New DocType" button, its `Enter DocType Name`/`Failed to register DocType`/`Select a DocType...` prompts and messages, the Dashboard's "DocTypes Registered" stat tile, the Prefix Configurations and pending-Approvals table column headers, and the Add-Field modal's "Target DocType" placeholder) and renamed them to "Record Type" - matches the ADMIN_GUIDE.md glossary's own plain-language phrasing ("DocType: a 'kind of record'"). Internal identifiers (`currentDoctype`, `renderDocTypeBuilderView`, `state.activeDoctypes`, code comments, the `doctype`/`document_type` API/DB field names) deliberately left alone, consistent with this repo's existing convention of renaming only user-facing labels. **Found but deliberately not touched**: `public/index.html`'s `#register-doctype-modal` (~line 456, "Register New DocType" title, "DocType Name" label, `submitRegisterDoctype`/`applyDocTypeTemplate` handlers) is dead markup - grepped `app.js` and confirmed nothing calls it; the live "Register New Record Type" button uses `openNewDoctypeModal()`'s sequential prompts instead. Never rendered, so not part of the user's "I can still see it" report - flagged here rather than silently deleted, since removing dead code is a separate decision.
*   **"Stores" master was completely empty - three compounding bugs found and fixed (2026-07-20, live user report from the Database Schema Design screenshot above)**: a DB query (`doctype_meta` LEFT JOIN `doctype_fields`) confirmed `Stores` was the *only* registered doctype anywhere in the system with zero fields. Digging into why surfaced two more, worse bugs on top of the missing fields: (1) it also had **zero `role_permissions` rows**, meaning every role including HR/Admin got a default-deny 403 (`handleCheckPermission`'s `sql.ErrNoRows` path) even after fields existed; (2) the sidebar's own "Stores" nav item (`menu-stores` click handler) set `currentDoctype = 'Store'` (singular) while the actually-registered doctype is named `'Stores'` (plural) - confirmed via `scripts/archive/patch.js`, an old archived patch script, that the original code correctly used `'Stores'`, so this was a regression typo introduced sometime after. Fixed all three in `db/migrations_stores_master_fields.sql` (7 fields - code/name/address/city/contact_phone/manager/status, mirroring `Location`'s shape; role grants copying `Location`'s exact HR/Admin-full / Store-Manager-read-only split; backfilled `module_key='inventory'` to match sibling `Bin`, since it was blank) plus the one-line `app.js` typo fix, applied live to `custom_erp`. **Noted, not acted on**: `Location` already has a `type` field with options `Store,Warehouse,HO` covering the same concept - `Stores` looks like a pre-`Location` (Stage 17.9) leftover never merged in. Left as-is since merging/deprecating a master is a separate decision from "make the broken one work," not something to decide unilaterally.

---

## 22. Standardized Error/Message Catalog (What We Built, 2026-07-20)

User supplied a 301-row spreadsheet (`ERP_Standard_Message_Control_Matrix_Final.xlsx`) standardizing every user-facing message across the whole ERP - message ID, error code, module, scenario, standard wording, severity, HTTP status, display style, retryable/log/audit-required flags, priority - and asked whether this "error master" already existed in the app, and to build/fix it if not. It didn't: errors were ad hoc, 205 plain-text `http.Error(...)` calls mixed with 152 hand-rolled JSON-error writes across 11 files, no codes, no consistent shape - and this surfaced a real live bug, since `apiMiddleware` sets `Content-Type: application/json` on every response but `http.Error`'s plain-text body silently broke every frontend `await res.json()` caller on that path (the user saw a generic fallback instead of the real message).

*   **Generated catalog, not hand-maintained**: `scripts/gen_error_catalog.py` (Python/openpyxl, dev-time only, no runtime dependency) reads the xlsx and emits `internal/server/error_catalog_generated.go` (302 entries - the original 301 plus one gap-fill row, `GLOBAL-0302` "Unexpected server error", since the source matrix had no row at all for the panic/500 path). Source xlsx backed up before appending the new row.
*   **Central response helper** (`internal/server/apierror.go`): `writeAPIError`/`writeAPIErrorGeneric` write the same `{"error", "code", "correlation_id", "retryable"}` JSON envelope everywhere, firing the *existing* `engines.LogSystemError`/`LogAuditEvent` per the catalog's Log/Audit-Required flags (code prefixed onto the logged message - no DB schema change needed).
*   **Framework-level wiring** (`internal/server/middleware.go`): panic recovery, rate limiter (`SEC-0280`), auth/token failures (`GLOBAL-0009`), `moduleGate` (`SAAS-0191`), `featureGate` (kept its existing fail-closed 403 rather than the matrix's softer prescribed 200 - flagged as a product decision, not silently changed).
*   **Full handler sweep**: all 400 existing error-response call sites across all 10 `internal/server/handlers_*.go` files converted (18 got a precise curated catalog code, 382 a generic-but-consistent fallback that preserves the original message text and HTTP status exactly). Delegated to parallel background agents, one per file/file-group, each required to never silently change a response's actual status code and to verify with `go build ./...` before finishing. **Real structural finding**: the catalog has zero rows at HTTP 400 (uses 422 uniformly for validation errors) and zero at 405, while a large share of the existing codebase uses exactly those statuses - a genuine 400-vs-422 convention gap between the matrix and the code, not a per-scenario matching failure, and not resolved unilaterally (flagged for a product decision).
*   **Frontend**: `apiFetch`/`apiUpload`'s 401/429 handling now reads the real error/code from the (now-reliable) response body instead of a hardcoded string. New `showToast()` and `renderPageBanner()` primitives for the matrix's two largest display styles that had no existing UI (Toast wired into the rate-limit path; Page Banner built and available, broader screen-by-screen adoption left as backlog - a UX polish task, not a correctness fix). Existing inline-field and modal-dialog patterns reused as-is.
*   **A concurrent session was actively writing a "Finance Maturity" feature (`handlers_finance_maturity.go` and others) throughout this work** - deliberately excluded that file from the sweep rather than risk colliding with in-progress uncommitted work; it gets the same treatment in a follow-up pass once it lands.
*   **Verified**: `go build`/`go vet`/`go test ./... -p 1` clean, including updating one stale test assertion (`TestModuleGateBlocksAndRestoresDoctypeAccess` now checks for the `SAAS-0191` code instead of the old dynamic message text it superseded - an intended behavior change, not a regression). Manually verified against a live throwaway instance (port 8101, confirmed free first): unauthenticated → `401`/`GLOBAL-0009`; bad login → `401`/`USERAC-0021`; rapid logins → `429`/`SEC-0280`, all with the correct standardized JSON body.
*   Full design, per-file stats, and the complete list of known matrix/codebase conflicts found: **[docs/specs/message_catalog.md](specs/message_catalog.md)**. Full checklist detail: `docs/micro_checklist.md` Stage 23.

---

## 23. Stage 20 Track B.3 Build Record — Finance Maturity (What We Built, 2026-07-20)

Third Stage 20 track (after Track B.1 POS Maturity §18 and Track B.2 WMS Maturity §20), run concurrently with the Stage 23 error-catalog session above (see its own note acknowledging this file). Full item-by-item detail: `docs/micro_checklist.md` Stage 20 Track B.3 (20.25-20.34); new migration `db/migrations_stage20c_finance_maturity.sql`; new engine files `engines/bank_reconciliation.go`, `engines/payment_proposal.go`, `engines/tds.go`, `engines/notes.go`, `engines/sales_invoice.go`; new `internal/server/handlers_finance_maturity.go`.

*   **20.25/20.26 Bank reconciliation** — new `BankAccount` master and `BankStatementLine` transaction doctype (one document per statement line, so the existing Stage 3.3 `BulkImportCSV` engine imports it with zero new code, same shape decision as Stage 20.20/20.21's `CycleCountLine`). `ReconcileBankStatement` matches Unmatched statement lines against not-yet-matched `gl_postings` on the account's own GL code, exact-amount + direction-aware, within a ±3 day window; a new additive `gl_postings.matched_statement_line_id` column tracks the GL-side match state.
*   **20.27 Payment proposals** — new read-only-by-generic-CRUD `PaymentProposal` doctype (same reasoning as Stage 20.7's `POSSession`). `CreatePaymentProposal`/`ExecutePaymentProposal` batch multiple `Matched` vendor invoices through the *existing* Stage 17.8 `PayVendorInvoice`, one at a time — a best-effort batch (each pay is its own atomic transaction) rather than one giant transaction, with per-invoice results reported back.
*   **20.28 TDS** — new `TDSSection` master seeded with standard 194C/194J/194Q sections. `PayVendorInvoiceWithTDS` is a deliberate sibling of `PayVendorInvoice` (not a branch inside it) so the existing no-TDS path and its tests stay untouched; splits the payment into net cash + a credit to new `2300 TDS Payable Account`.
*   **20.29 GST return summary** — report-only GSTR-1/3B-shaped aggregation derived entirely from existing `gl_postings` (account 4100's net balance = taxable value; 2200/2201/2202 net credit balances = CGST/SGST/IGST), no new data collection.
*   **20.32 Debit/credit notes** — new `DebitNote`/`CreditNote` doctypes, each Draft→Posted via its own GL-reversing post function against two new contra accounts (`5150 Purchase Returns & Allowances`, `4150 Sales Returns & Allowances`) rather than reusing 1200/5100, which already carry a sale-specific COGS meaning elsewhere.
*   **20.33 Receivables ageing — the interesting one.** Building this exposed that `SalesInvoice` had existed since Stage 1 as a registered Transaction doctype with a real status flow (`Draft/Approved/Paid/Cancelled`) but zero amount field, zero GL posting, and zero frontend — a dormant shell, not a working credit-sales flow (this codebase's POS settles in cash/card/UPI at checkout; nothing before this ever created a real, ageing receivable). Rather than invent a parallel doctype, new `engines/sales_invoice.go` wires the existing shell up for real: `PostSalesInvoice` (Draft→Approved, books AR debit 1300/credit 4100) and `SettleSalesInvoice` (Approved→Paid, clears it) — symmetric to how `PayVendorInvoice` already works on the payable side. `GetReceivablesAgeingReport` then buckets Approved SalesInvoices by age exactly the way the existing `GetPayablesAgeingReport` already buckets Approved PurchaseOrders.
*   **20.34 Period-close checklist** — `GetPeriodCloseChecklist` runs read-only pre-close validations (trial balance balanced, nothing Pending Approval, no VendorInvoice in MismatchHold, no unmatched BankStatementLine in-period) on top of the existing Stage 17.4 `CloseAccountingPeriod` — advisory only, never blocks the actual close. Along the way, closed a real pre-existing gap: Accounting Periods had zero frontend despite the create/list/close API existing since Stage 17.4 (same shape as the Transfers/Inventory/Users/Roles gaps Stage 22 fixed) — new "Accounting Periods" tab added to the Finance/GL screen.
*   **Frontend**: 5 new screens under the Finance module flyout (Vendor Invoices, Payment Proposals, Bank Reconciliation, Debit/Credit Notes, Sales Invoices) plus 2 new Reports tabs (Receivables Ageing, GST Return Summary) plus the Accounting Periods tab above. Every new doctype's *creation* deliberately reuses the existing generic doctype-table New-record form (fields already registered via the migration) rather than a parallel create form — these screens only add the actions the generic CRUD screen can't express (match/pay/reconcile/execute/post/settle).
*   **A vendor-invoice-adjacent gap found and closed in passing**: `VendorInvoice` itself (Stage 17.8's 3-way match + pay) had existed since that stage with zero frontend of its own — the new Vendor Invoices screen was a prerequisite for Payment Proposals/TDS to be reachable/testable through the UI at all, not scope creep; "Second principle" in `CLAUDE.md` calls this out explicitly (a feature isn't done until it's usable).
*   **Verified**: `go build`/`go vet`/`go test ./... -p 1` clean (also re-confirmed clean after a concurrent session's in-flight edit to `handlers_admin_identity.go`/`middleware.go` passed through a transiently-broken intermediate state mid-session — waited, rebuilt, confirmed it self-resolved, never touched those files). Migration applied live to `custom_erp`. Live-verified end-to-end against a throwaway port-8099 instance logged in as `manager1` (Store Manager): created and matched a vendor invoice, paid it with TDS and confirmed the exact expected 3-way GL split (debit 2100 ₹50,000 / credit 1100 ₹45,000 / credit 2300 ₹5,000); posted a debit note and a credit note and confirmed their GL entries; posted then settled a sales invoice and watched Receivables Ageing pick it up and then clear; batched a second matched invoice into a payment proposal and executed it; reconciled a bank statement line against a real GL posting and confirmed the match recorded on both sides; pulled a real period-close checklist that correctly flagged one genuine pre-existing Pending-Approval document. `node --check public/app.js` clean; every new DOM element ID cross-checked programmatically against its `getElementById` call site (no orphans). All throwaway test data (a temporary Vendor, two VendorInvoices, a DebitNote, CreditNote, SalesInvoice, BankAccount, BankStatementLine, the payment proposal, and a test accounting period) deleted from the shared `custom_erp` DB afterward; the throwaway server binary and its logs removed.
*   **Not done this pass**: 20.30 (e-invoice/IRN) and 20.31 (e-way bill) remain blocked on real GSP credentials, unchanged from the original scoping. Track B.4 (Reports Engine, 20.35-20.40) is still open.

---

## 24. Stage 20 Track B.4 Build Record — Reports Engine (What We Built, 2026-07-20)

Fourth and final Stage 20 track, built immediately after Track B.3 (Finance Maturity, §23) in the same session. Full item-by-item detail: `docs/micro_checklist.md` Stage 20 Track B.4 (20.35-20.40); new files `engines/report_registry.go`, `engines/report_definitions.go`, `engines/report_export.go`, `internal/server/handlers_report_engine.go`, `db/migrations_stage20d_reports_engine.sql`.

*   **20.35 The framework itself.** `ReportDefinition` (columns, params, a `Run` function, an optional `DrillDown`) registered once per report via `RegisterReport` — deliberately not a full ad hoc SQL query builder (an injection-risk feature outside this ERP's scope), just a fixed Go function described as data so one generic frontend screen can drive any of them. All 6 pre-existing reports wrapped as-is (the old tabs/endpoints stay untouched, purely additive); `RunReport` validates required params and applies column masking centrally.
*   **20.36/20.37 Saved filters and async export both reuse existing patterns rather than adding new mechanisms.** `ReportFilterPreset` is a plain registered doctype — saving/listing a filter needed zero new backend code, just the existing generic `/api/v1/doc/ReportFilterPreset`. `ReportExportJob` + `StartReportExportWorker` reuse the exact same ticker/per-tenant-schema-poll shape as `StartOutboxWorker`/`StartPatchIntakeWorker`, and store the generated CSV directly in the job's own JSONB document (Stage 15.2's `ImportJob.error_csv` trick) rather than adding file storage.
*   **20.38 Drill-down** re-derives the summary report's own bucket-boundary logic rather than keeping a second copy of it, so a drill-down can never disagree with the summary it's drilling into.
*   **20.39 Column masking** redacts Sensitive-flagged columns to `•••` in place (not omitted) for any role below Store Manager/HR-Admin — a real, live-testable version of the master plan's "report export leaks finance data" loophole control, applied to the new framework; the pre-existing per-report endpoints (Payables Ageing, GST Summary, etc.) don't have this yet, noted as a natural follow-up rather than silently claimed as covered.
*   **20.40 Catalog expansion** added 7 of the master plan's 10 candidates (GRN Register, Cash Book, Bank Book, Asset Register, Loyalty Ledger Summary, Production Order Status, RFQ Comparison — the last three wrap already-existing endpoints/functions rather than duplicating them). **Two candidates deliberately skipped, not deferred silently**: Attendance Summary has no underlying data model anywhere in this codebase (would be a new HR sub-module, not a report); Stock Ledger's own doctype/write function (`StockLedgerEntry`/`WriteStockLedgerEntry`) exist but are genuinely dead code — never called from GRN/checkout/transfer/putaway/cycle-count — so there's no real movement history to report on without first wiring up 5+ already-verified flows, judged too large for this pass.
*   **A mid-build user request, addressed inline**: "Fix doctypes. keyword from entire erp... We have removed this terminology from ui and docs." Investigation found a concurrent session (§21's 21.12 entry) had already swept almost everything to "Record Type," explicitly flagging one remaining gap: `index.html`'s `#register-doctype-modal`, dead markup nothing in `app.js` ever called. Deleted it (confirmed zero references first, not a rename). Also swept `ADMIN_GUIDE.md`/`README.md`/`UAT_CHECKLIST.md` (not covered by 21.12) from "DocType" to "Record Type," while deliberately leaving `framework_architecture.md` alone (added a scoping note instead) since it's a deep technical doc explicitly comparing to ERPNext/Frappe's own DocType concept and describing the real `doctype_meta`/`doctype_fields` tables — renaming its prose would misdescribe the actual code.
*   **Frontend**: one new "Report Catalog" tab under Reports, entirely metadata-driven from `GET /api/v1/reports/catalog` — a report picker, a dynamic param form, a generic result table, a saved-filter dropdown, and a background-export button with poll-until-complete + authenticated Blob download (a plain `<a href>` link can't carry the required Bearer token; fetched via `apiFetch` + `URL.createObjectURL` instead — the existing PIM import-errors download link uses the broken `<a href>` pattern, a pre-existing latent bug not touched this pass).
*   **Verified**: `go build`/`go vet`/`go test ./... -p 1` clean, migration applied live to `custom_erp`. Live-verified end-to-end against a throwaway port-8099 instance as `manager1`: catalog listing; ran current-stock/payables-ageing/cash-book/asset-register/loyalty-summary/production-order-status; confirmed a missing-required-param 400 on rfq-comparison; drilled down into a receivables-ageing bucket and a GST-summary window and got exactly the right underlying rows; saved and listed a filter preset; queued a background export, watched the worker complete it, and downloaded a CSV that exactly matched the live report. All throwaway test data removed afterward.

**Stage 20 (ERP Maturity Master Plan Execution) is now fully closed except**: Track A (20.1-20.5, blocked on user-supplied input) and 20.30/20.31 (blocked on real GSP credentials).

For the current build tracker (what's still open), see **[docs/micro_checklist.md](micro_checklist.md)**.
